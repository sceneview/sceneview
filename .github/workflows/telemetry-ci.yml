name: Telemetry Worker CI

# CI validation for the telemetry-worker Cloudflare Worker.
# Runs typecheck + unit tests on every push/PR that touches telemetry-worker/.

on:
  push:
    # `branches: [main]` is required — `paths:` alone does NOT scope the push
    # trigger to a branch, so without it this workflow runs on every push to
    # every branch (each `claude/*` worktree branch). PR coverage is already
    # provided by the `pull_request` trigger below.
    branches: [main]
    paths:
      - 'telemetry-worker/**'
  pull_request:
    paths:
      - 'telemetry-worker/**'
  workflow_dispatch:

# One run per ref; cancel older in-progress runs to save resources.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Typecheck & test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          # Pinned to 20 to match every other Node-using workflow
          # (ci.yml, release.yml, device-qa.yml, render-tests.yml) — #1602.
          node-version: 20
          cache: npm
          cache-dependency-path: telemetry-worker/package-lock.json

      - name: Install dependencies
        working-directory: telemetry-worker
        run: npm ci

      - name: Typecheck
        working-directory: telemetry-worker
        run: npx tsc --noEmit

      - name: Test
        working-directory: telemetry-worker
        run: npx vitest run
