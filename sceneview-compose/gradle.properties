######################
# MavenCentral Publish
######################
POM_NAME=SceneView Compose Multiplatform
POM_ARTIFACT_ID=sceneview-compose
VERSION_NAME=4.40.0
# Own description: this artifact is the viewer subset, not the whole SDK. ASCII only —
# Gradle reads .properties as ISO-8859-1, so an em dash is published as mojibake.
POM_DESCRIPTION=Compose Multiplatform 3D viewer for SceneView - one SceneViewer composable from commonMain, rendered by Filament on Android and desktop and by RealityKit on iOS.
