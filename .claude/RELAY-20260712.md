# RELAY 2026-07-12 — strange-goldberg (03:00 CEST)

> Complète [RELAY-20260711.md](RELAY-20260711.md) (vigorous-hofstadter, orchestrateur
> canonique — release 4.22 = à lui SEUL, worktrees splat/iOS intouchés).

## ÉTAT COURANT (MAJ 2026-07-12 22h55 — clôture strange-goldberg, fin de fenêtre Fable)

- **Voie strange-goldberg FERMÉE — ZÉRO item en vol.** Tout livré et mergé : docs
  post-#2653 (sans commit, déjà in-PR #2655) · flake #2665 (PR #2666) · **#2645 CLOS :
  PR #2685 squash-mergé sur main, claim released** (QA visuelle complète, CI verte ;
  follow-up port iOS sécurisé en **issue #2700**).
- Successeur Opus : le brief canonique + prompt de spawn sont dans
  [RELAY-20260711.md](RELAY-20260711.md) § « HANDOFF FIN DE FENÊTRE FABLE ». Rien à
  reprendre de cette voie ; détail de clôture en MAJ 22h55 (fin de fichier).

## Fait cette nuit (vérifiable)

- **#2643 CLOS** — PR #2677 squash-mergé sur main (`939a1e1fd`), CI verte au moment du
  merge, claim released (`claim.sh --release 2643`). vigorous : ton étape « #2643 : PR
  si vert » est donc terminée ; le rebase local de l'agent mort était superflu (aucun
  conflit), poubellisé.
- **#2665/#2666 (flake `SketchfabServiceTest` cancellation)** — mergé le 07-11 :
  `executeCancellably` re-canonise l'annulation (`ensureActive` sur IOException). Le
  flake qui a fait dérailler la CI du PR #2655 est éradiqué de main.
- **Docs post-#2653** — tâche d'origine de cette session CLOSE SANS COMMIT : la refonte
  « contrat transform unique » a été livrée in-PR #2655 par amazing-neumann (vérifiée
  contre `SceneScope.kt` de main : 70 DisposableEffects keyés, aucun write de transform
  en SideEffect, llms.txt sain). Brouillon local supprimé (récupérable : reflog
  `bad559a3b` du worktree strange-goldberg).
- **Disque : 2,5 → 17 Gi libres** — purge des clones scratchpad morts (2× 1,6 G +
  résidus). Le gate gradle (<6 Gi) est LEVÉ.

## En vol (strange-goldberg)

- **#2645 multi-source Explore** — wip sécurisé sur origin
  (`feat/2645-explore-multi-source` @ `c0c7d9be`), agent Opus finisher lancé ~03:00 :
  reprise du wip → implémentation → gates gradle → QA visuelle émulateur → PR
  (auto-merge seulement si QA visuelle faite, sinon DRAFT explicite). Claim #2645 posé
  via claim.sh. **Ne pas dupliquer.**
  Vérif : `gh pr list -R sceneview/sceneview --search "2645"` ·
  `bash .claude/scripts/claim.sh --check 2645`.
  → **CLOS 22h55 : PR #2685 mergé, claim released** — voir MAJ en fin de fichier.

## Pièges reconduits

- Gradle : 1 seul build lourd android-demo à la fois (locks `~/.gradle`).
- Émulateur : RAM-gate de `setup-ar-emulator.sh` fait foi — ne pas forcer.
- ⛔ Surface publique (README l.24 / l.501-509, nav site) : cleanup séquencé
  profile-com en attente de validation Thomas — coordonner avant de toucher.

---

## CLÔTURE voie kind-varahamihira (fin de fenêtre Fable, 07-12 soir) — handoff Opus

Session archivée. Tout le travail de cette voie est mergé/released SAUF un item EN VOL :

### EN VOL — PR #2676 (#2581, ombre au sol viewer Sketchfab) : gate QA run3
- **État** : fix COMPLET poussé (`4e299f3e` = quad XZ + `isOpaque = false` sur le SceneView du
  viewer — le shadow-multiply noir n'avait rien à assombrir sur fond noir opaque ; translucide =
  ombre sur la sheet, l'intent d'origine). Review-fanout OK (0 ERROR). Auto-merge VOLONTAIREMENT
  désarmé : run2 a prouvé (sonde pixel) que le quad seul restait invisible ; run3 doit prouver le
  fix complet. Claim #2581 released (la PR ouverte = le lock de fait).
- **Prochaine action exacte** (~10 min, session résourcée : ≥6 GB disque, émulateur libre) :
  QA différentielle BASELINE `a8b072d1` vs AFTER `4e299f3e` — Explore → Trending →
  « FREE 1975 Porsche 911 (930) Turbo » → Open in SceneView, sonde pixel bande sol
  (clé Sketchfab : memory `reference_sketchfab.md` → local.properties gitignored du clone /tmp,
  jamais committée). Patch d'ombre mesurable sur AFTER ⇒ `gh pr merge 2676 --squash --auto`.
  Ombre absente ⇒ poster le diagnostic, ne pas merger. Protocole détaillé + historique :
  commentaires de la PR. Artefacts : `qa-artifacts-2581/` (racine du repo principal, non commité).
- **Re-vérification** : `gh pr view 2676 --json state,autoMergeRequest` ·
  `git log origin/fix/2581-sketchfab-shadow-quad -1`
- Follow-up hardening du receiver (culling/AABB, recette ShadowReceiverPlaneNode) : issue #2699.

### Reste de la voie (tout sécurisé, rien à reprendre)
- Mergés cette fenêtre : #2650 (+#2639) · #2637 (+#2626) · #2584 (#2241 5/6) · #2667 (+#2632) ·
  #2674 (#2482 item 4) · #2682 (#2612 P1 store-preflight, en daily dans maintenance.yml).
- Design docs décisionnels postés (gatés Thomas) : #2540 (desktop offscreen → piste contributeur
  filament-kmp) · #2612 (P2 = screenshots ASC + Data safety CSV) · #2482 (close-out = passe
  device 5 points, cf. commentaire d'audit).
- Fermés avec preuve : #2629, #2621 (déjà fixés 4.21.2).
- Chip UI possiblement encore affiché pour #2653 : STALE (fixé via PR #2655) — cliquer dessus
  serait un no-op, à ignorer/dismisser.

### Post-Fable (vérifié à la clôture)
- AUCUN hard-pin `fable` dans agents/workflows/skills (sv-* = opus/sonnet ; règle §9 en place).
- Scheduled tasks : toutes disabled ou one-shot déjà tirées — rien ne casse.
- Successeur : session FRAÎCHE avec ce RELAY comme brief (jamais resume/SendMessage — perd
  l'override modèle).

---

## CLÔTURE voie zealous-lamport (fin de fenêtre Fable, 07-12 après-midi) — handoff Opus

Mandat : les 2 follow-ups de #2657/#2683 (guard bibliothèque PlacementScene + sweep doc).
**Tout est mergé — zéro reprise de code nécessaire sur cette voie.**

### Fait (vérifiable)
- **PR #2690 MERGÉ** sur main (`984eaa73c`, squash, 15:02 UTC) : `PlacementScene` enforce
  lui-même l'exclusion des deux shadow receivers coplanaires (prédicats purs
  `shouldRenderPlaneGrid` / `shouldCatchGroundShadows`, miroir du pattern #2683) + sweep doc
  du drift pré-existant (llms.txt recipe + signature, KDoc `ShadowReceiverPlane`
  ARSceneScope.kt, agents recipes.md, bonus llms-full.txt). Nouveau
  `PlacementSceneShadowReceiverTest` 5/5 (invariant exhaustif 8 combos × counts 0-25).
  Review-fanout 4/4 PASS (0 ERROR, 0 warning, breakingApi=false), CI verte, changelog
  fragment `changelog.d/2657-placementscene-single-shadow-receiver.md`.
  Vérif : `gh pr view 2690 -R sceneview/sceneview --json state,mergeCommit`

### EN VOL — audit sceneview-web double-receiver (session SÉPARÉE, démarrée par Thomas)
- Origine : propagation du review-fanout #2690 — vérifier que le path web (Filament.js,
  même moteur) ne peut pas empiler deux receivers `shadowMultiplier` coplanaires. Un
  reviewer affirme que l'équivalent PlacementScene/ShadowReceiverPlane N'EXISTE PAS côté
  web — à vérifier contre le source, no-op documenté si confirmé.
- Chip `task_3cd94c0a` cliqué par Thomas → session locale indépendante en cours à la
  clôture de cette fenêtre. **Ne pas dupliquer si elle a abouti.**
- Vérif : `gh pr list -R sceneview/sceneview --search "sceneview-web shadow"` ·
  `gh issue list -R sceneview/sceneview --search "web shadow receiver"` ·
  ground truth : `grep -rn "shadowMultiplier\|shadow_receiver" sceneview-web/src/`
- Si la session du chip est morte sans trace (aucun PR/issue/commit) : re-lancer l'audit —
  d'abord établir si un plane-renderer ET un shadow-catcher peuvent coexister sur la même
  surface dans sceneview-web ; si impossible aujourd'hui → no-op documenté (une ligne,
  pas d'issue) ; si possible → mirrorer le guard #2690 (compile+tests+changelog+review).

### Hygiène
- Worktree `zealous-lamport-0a9483` : PROPRE (aucun commit local, branche session jamais
  poussée, tout le travail est passé par un clone /tmp aujourd'hui supprimé) → prunable
  via `worktree-auto-prune.sh`.
- Branche `claude/2657-placementscene-receiver-guard` : supprimée au merge (auto).

---

## MAJ 2026-07-12 22h55 — strange-goldberg (CLÔTURE, fin de fenêtre Fable)

- **#2645 CLOS** — PR #2685 squash-mergé sur main, branche supprimée, claim released [gh].
  Chaîne complète : finisher Opus (`726e5224`, abstraction ModelSource Sketchfab | Icosa
  Gallery | Poly Haven + 24 tests) → review-fanout MERGE_AFTER_WARNINGS (0 ERROR confirmée,
  6 warnings) → fixes in-PR (`83b8c909` CI validate-demo-assets, `891c70ba` hardening réseau
  single-flight/caps/sanitisation + CTA `downloadable` gaté, `300c165e` samples.md,
  `8962578d` placeholder recherche source-aware — finding attrapé par la QA visuelle) →
  QA visuelle émulateur COMPLÈTE (feeds CC Icosa/Poly Haven réels, recherche cross-source,
  état vide propre, 0 crash — preuves scratchpad `qa-2645/` 01-08) → CI toute verte →
  merge orchestrateur.
  Vérif : `gh pr view 2685 --json state,mergedAt` · `gh issue view 2645 --json state`.
- **Follow-up sécurisé en issue** : port iOS multi-source Explore = **#2700** [gh]
  (checklist complète : ModelSource Swift, sanitisation cache, placeholder source-aware,
  commentaires périmés ExploreTab.swift:515/796, bounded reads).
- **ZÉRO en vol pour cette voie.** Worktree strange-goldberg == origin/main (aucun commit
  unique ; brouillon docs abandonné récupérable au reflog `bad559a3b`). Aucun cron, monitor
  ni agent background actif côté strange-goldberg [harness].
- Post-Fable : successeur = session FRAÎCHE (jamais resume/SendMessage — perd l'override
  modèle) ; brief canonique + prompt de spawn = RELAY-20260711.md § HANDOFF. emulator-5554
  était partagé avec la QA arcamera d'une autre session — vérifier le focus avant de le
  tuer (`adb -s emulator-5554 shell dumpsys window | grep mCurrentFocus`).

---

## CLÔTURE voie mcpize-count (trimui-game-cleanup, fin de fenêtre Fable, 07-12 soir) — handoff Opus

Session close — **zéro item en vol sur cette voie**. Tout est mergé ou tracké par issue :

- **PR #2689 MERGÉE** (squash `105714a`) — mcpize.yaml « 35 Pro tools » → 27, compté par
  import réel de `mcp/src/tiers.ts` (jamais regex), + retrait de « multi-platform setup » du
  descriptif Pro (Free depuis MCP 4.0.5).
  Vérif : `gh pr view 2689 --json state,mergedAt`
- **Chip follow-up (task_c3a6eb52) livré par sa propre session** : PR #2696 MERGÉE — gateway
  monte 67 tools, phantom `get_started` retiré du free tier, et 2 tests de vérité
  (`mcp/src/tool-count-claims.test.ts` + `mcp-gateway/test/tool-count-claims.test.ts`) qui
  verrouillent yaml ↔ code. Restent 2 issues OUVERTES, self-contained :
  - **#2697** — 11 tools gateway hors tier map (default-to-pro silencieux) + `PRO_UPGRADE_MESSAGE`
    périmé. Fix sketch complet dans l'issue ; changer `PRO_TOOLS.length` change la copy publique
    ⇒ change review-é dédié.
  - **#2698** — worker gateway déployé 3 tools en retard ⇒ `wrangler deploy` **MANUEL, gaté
    Thomas** (aucune session ne doit le faire seule).
  Vérif : `gh issue view 2697 --json state` · `gh issue view 2698 --json state`
- **Piège successeur** : `view_3d_model` absent du paquet npm local = **VOULU**
  (`GATEWAY_ONLY_FREE_TOOLS`, documenté dans `mcp/src/tool-count-claims.test.ts`) — ne pas
  « re-fixer ». Le yaml dit 28 free (tools servis localement), l'array en liste 29 : cohérent, testé.

### Résidus disque constatés au balayage global (dirty ⇒ jamais auto-pruné, rien ne se perd)
- worktree `kind-varahamihira-f1485e` : 62 fichiers dirty — wip outillage (`impact-check.sh`
  modifié + `test-impact-check.sh` nouveau, workflows CI), dernier commit 06-02. Session déclarée
  archivée ci-dessus sans mention de ce wip → à trier ou jeter post-Fable.
- worktree `pedantic-robinson-cdb53e` (branche `fix-2235`) : 73 dirty, stale depuis 05-27 —
  chantier repris par la PR #2676 (voie kind-varahamihira). Candidat poubelle après merge de #2676.
- `ar-model-viewer-android` : spec onboarding-fork committée-poussée par sa propre voie
  (`2a6849f`) — RAS.
- Pins `fable` : re-vérifié zéro hard-pin (agents/, workflows/, settings) — seuls hits = la règle
  « never hard-pin fable » de workflows/README.md §9.

---

## MAJ 2026-07-13 ~00h20 — nice-mirzakhani (voie MCP) : clôture + filet final de fenêtre

Voie MCP : **déjà couverte** par la clôture mcpize-count ci-dessus (PR #2696 MERGÉE,
follow-ups **#2697** agent-fixable / **#2698** wrangler deploy manuel gaté Thomas). Zéro en
vol. Le balayage final de fenêtre (protocole /model-handoff, outillé par commandes) a trouvé
et traité deux trous résiduels :

- **Bump v4.22.0 atterri sur main** (`760e1ecb0`, merge zéro-conflit via
  merge-tree/commit-tree, aucun force-push) : le tag était publié et les workflows partis,
  mais le commit de bump n'était atteignable QUE depuis le tag — `gradle.properties` sur
  main disait encore 4.21.2, ce qui aurait fait mentir la source-of-truth de tout successeur.
  Vérif : `git fetch origin main --tags && git merge-base --is-ancestor v4.22.0 origin/main
  && grep VERSION_NAME gradle.properties`.
  Reste release (consigne vigorous reconduite) : `/store-status` post-propagation — si
  Maven/npm encore 404 à T+2h du tag, inspecter le run Release.
- **Branche locale `claude/agitated-tereshkova-7a8fe1` : 50 commits jamais poussés**
  (fix iOS « Sketchfab → USDZ, App Review 2.1(a) » #2252, ~6 semaines, documentée nulle part
  ailleurs). Volontairement NON poussée (contenu de 6 semaines non re-validé) — auditer avant
  toute purge locale : `git log origin/main..claude/agitated-tereshkova-7a8fe1 --oneline`.
  L'issue #2252 [gh] reste la référence du blocage App Store (accord Apple = gate Thomas).

Spawn prompt successeur : le canonique reste RELAY-20260711.md § « HANDOFF FIN DE FENÊTRE
FABLE ». Variante voie MCP : « [sceneview] reprise MCP post-handoff — lis RELAY-20260712
§ mcpize-count + § nice-mirzakhani, prends #2697 (agent), rappelle #2698 à Thomas. »

---

## MAJ 2026-07-17 — wizardly-feynman (triage PRs dependabot) : CLÔTURE

Mandat : triage + merge des PRs ouvertes (état mesuré 07-16 17:05). Constat en session :
Thomas avait déjà squash-mergé #2701, #2704, #2705 (TS 7 majeur), #2706, #2707 et #2717
(07-16 17:07–17:09 CEST), et la session blissful-mayer a validé (verifyRoborazziDebug
local, branche pr-2703) puis mergé #2703 (roborazzi 1.68, `8e8289080`). Fait par cette voie :

- **#2605 (detekt 2.0.0-alpha.5) FERMÉE avec motif** : l'alpha fait échouer l'analyse sur
  du code inchangé (12 issues `:sceneview-core` commonMain, 7 `:sceneview`, + arsceneview —
  CyclomaticComplexMethod/LongMethod/NestedBlockDepth, ex. `LightEstimator.update`,
  `ARSessionFailure.from`). Adopter = baseline detekt ou refactors dédiés, pas un bump
  auto. Dependabot re-proposera une alpha ultérieure.
  Vérif : `gh pr view 2605 --json state`
- **#2702 (play-services-location 21.3.0→21.4.0 — consommé uniquement par
  samples/android-demo) MERGÉ squash → `4dda48422`** et **#2602 (compose-bom
  2026.05.01→2026.06.01) MERGÉ squash → `f9412205e`** après : CI verte re-vérifiée
  (11 checks dont « Build libraries & samples ») + compile locale verte
  `:sceneview:compileReleaseKotlin :arsceneview:compileReleaseKotlin` sur branche
  combinée des deux cherry-picks (BUILD SUCCESSFUL 12m12s, à froid).
  Vérif : `gh pr view 2702 --json state,mergedAt` · `gh pr view 2602 --json state,mergedAt`
- **#2718 (Filament 1.72.1) NON touchée** — session dédiée (worktree adoring-easley,
  branche gate/2718-integration), invariant .filamat. Dernière PR ouverte du triage.
- Hygiène disque (gate <6 Gi bloquait les builds ; le disque oscillait 4,9–9 Gi avec
  plusieurs sessions actives) : purge des scratchpads /tmp des sessions closes
  (kind-varahamihira, nice-mirzakhani, vigorous-hofstadter, ~1,6 G) + clone lean mort
  `sv-2645` (2,3 G, PR #2685 mergée 07-12) VIDÉ en préservant le répertoire lui-même —
  c'est le cwd de l'emulator-5554 du pool, leasé par une session active à ce moment ;
  l'émulateur n'a PAS été touché ; les preuves QA `qa-2645/` sont conservées. Caches
  npm/playwright/yarn purgés. ⚠️ scratchpads strange-goldberg (reste) et
  amazon-ar-placement : sessions actives, intacts. `worktree-auto-prune.sh --dry-run` :
  rien de prunable (tout actif/locké/unmerged).
- Worktree wizardly-feynman : branche de test `tmp/deps-2702-2602-test` supprimée après
  merge, retour sur la branche session. `local.properties` (sdk.dir) recréé dans le
  worktree — il manquait (gitignored), cause du premier échec de build « SDK location
  not found ».

**ZÉRO en vol pour cette voie.** Reste du paysage PR : #2718 seule ouverte (sa session).
