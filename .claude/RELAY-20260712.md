# RELAY — 2026-07-12 · fin de fenêtre Fable → reprise Opus

> Note de passation inter-sessions. Session émettrice : **lucid-dewdney-e281aa**
> (branche `claude/zen-saha-22a0ae`). Fenêtre Fable fermée → Opus prend le relais.
> Vérifie chaque état par la commande indiquée avant d'agir.

## Mandat de la session (fait)

Parti d'une QA terrain Pixel 9 (screenshots + screenrecord de Thomas) → 2 bugs filés →
un bug (#2657) a révélé une **classe** → audit-sweep parallèle → 3 fixes Fable sérialisés
→ 3 reviews Opus adversariales. Fenêtre Fable rentabilisée + Opus ciblé sur framing/review.

## État vérifié (commandes, 2026-07-12 ~17h CEST)

- **3 PRs prêtes, APPROVE Opus ×3, mergeable/CLEAN, NON mergées** (gate = device-QA, différé) :
  - `gh pr view 2678 -R sceneview/sceneview` — EnvironmentLoader ×3 : Filament-JNI sous `withContext(Main)` (#2669/2670/2671). CI pass. Verdict Opus en commentaire PR.
  - `gh pr view 2686 -R sceneview/sceneview` — PoseNode : SideEffect nu → DisposableEffect keyé 7-scalaires (#2672). CI pass. Miroir fidèle de #2650/#2639.
  - `gh pr view 2687 -R sceneview/sceneview` — web loadEnvironment : flag `destroyed` anti-UAF (#2673). CI pass. **web = leg BLOQUANT** par politique.
- **Umbrella** `gh issue view 2668` — classe « async continuations touch renderer/native state ». 5 HIGH filés (#2669-2673), 2 MED inline (SmoothTransform.kt:63, WebXRSession.kt:280).
- **Follow-ups filés (durables, GitHub)** :
  - `#2691` — UAF **frère** dans `loadModel` (fetch GLB initial `createAsset`/`addEntities` non gardés ; `superseded` ne couvre que `loadResources`). **Dépend de #2687**. Trouvé par la review Opus de #2687.
  - `#2688` — flake `RerunBridgeTest` : `withTimeout` n'interrompt pas un `readLine()` socket bloquant → hang >30 min.
- **Chaîne initiale NON corrigée (backlog)** :
  - `#2657` — double shadow receiver (le bug Pixel 9 déclencheur). **Exige une sonde différentielle émulateur/device AVANT fix** (règle reproduce-before-fix : `planeRenderer=false` vs bloc `ShadowReceiverPlane` retiré). Pas lancé en autonomie.
  - `#2658` — Rerun Debug : « Save & Share » actif sans bridge → message API brut. Fix = gater le CTA + message honnête (sidecar 127.0.0.1:9876 inatteignable en wireless).

## Prochaine action exacte (pour Opus)

1. **Décision merge des 3 PRs** (gate device-QA). Le device-QA local n'a PAS pu tourner :
   host resource-starved 2026-07-12 (RAM libre **0.66 Gi** < gate pool 3072 Mo ; disque **5.7 Gi** < gate 6 Gi ; émulateur-5554 pris par la session **arcamera** `com.gorisse.thomas.arcamera` — NE PAS killer). Options ouvertes proposées à Thomas, non tranchées :
   - (a) web-leg seul (bloquant, sans émulateur) quand disque >6 Gi : `bash .claude/scripts/device-qa.sh --platform=web`
   - (b) différer tout au checkpoint `/release`
   - (c) merger sur CI-vert + Opus-APPROVE (option B initiale de Thomas), device-QA au checkpoint.
   → **Reprendre par : re-vérifier RAM/disque, puis exécuter le choix de Thomas.**
2. Après merge de #2687 → traiter **#2691** (le `destroyed` flag existera).

## Pièges (⚠️ certains reconstitués, à re-vérifier)

- ⚠️ **Host chroniquement plein** : `df -h /` a oscillé 18 Gi → 5.7 Gi en 1 h (pression externe multi-sessions : ~18 worktrees actifs). Toujours `df -h` + `vm_stat` AVANT tout build/QA. `pre-gradle-disk-gate.sh` bloque <6 Gi.
- ⚠️ **6 issues #2668-2673 restent `in-progress`** (claims de cette session) : c'est VOULU (PRs ouvertes, merge en attente). Ne pas les libérer avant merge.
- ⚠️ Le bug hook `pre-gradle-disk-gate.sh` peut faire un **faux positif sur le mot « gradlew » dans un body de PR** (vu sur #2678) → utiliser `--body-file`.
- Reviews Opus : task-outputs en `/tmp` éphémère → **verdicts déjà recopiés en commentaires PR** (durables).

## Vérif rapide reprise

```
gh pr list -R sceneview/sceneview --state open --search "2678 OR 2686 OR 2687" --json number,mergeable,mergeStateStatus
gh issue view 2668 -R sceneview/sceneview --json state,title
df -h / | tail -1 ; vm_stat | awk '/Pages free/{print $3}'
cat .claude/STATE.md   # (gitignored, même host)
```
