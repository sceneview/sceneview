# SceneView — Session State

> **The single live source of truth for "where are we right now".** A fresh session reads
> THIS first (see `## BOOTSTRAP`). Gitignored, local, same-host — this `.template` is the
> tracked **seed**: on a fresh checkout run `cp .claude/STATE.md.template .claude/STATE.md`
> (or just run `claim.sh`, which auto-seeds it). The durable cross-host shadow of the
> `IN-FLIGHT` ledger is the GitHub `in-progress` label. Schema + claim protocol:
> [`.claude/workflows/README.md`](workflows/README.md) §5. Updated silently after every
> meaningful step; done `NOW` bullets **move** to `.claude/handoff.md`.

## NOW
<!-- ≤8 bullets: released version · what just shipped · what's broken -->
- _(seed — populate on first session: released version · what just shipped · what's broken)_

## IN-FLIGHT
<!-- the claim ledger — `claim.sh` owns this table; one row per actively-worked issue -->
| Key | Branch / worktree | Session | Claimed | Status |
|---|---|---|---|---|
<!-- claim-rows: claim.sh inserts new rows immediately ABOVE this line -->

## NEXT
<!-- ≤6 ordered followups, each links exactly one GitHub issue -->
1. _(seed — ordered followups, each a GitHub issue link)_

## BOOTSTRAP
<!-- the commands a fresh session runs — target <2 min, no scrollback read -->
```
cat .claude/STATE.md
bash .claude/scripts/sync-versions.sh
gh issue list --repo sceneview/sceneview --label in-progress
```
