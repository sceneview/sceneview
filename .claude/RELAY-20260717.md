# RELAY 2026-07-17 — voie quirky-hellman : #2648 Point & Ask P1 SHIPPED + diagnostic #2754

> Session dédiée au gros chantier #2648 (NEXT n°3 du RELAY-20260716). Claim pris puis LIBÉRÉ
> en fin de session. Aucune collision avec les voies précédentes.

## FAIT (vérifiable par commande)

- **#2648 P1 « Point & Ask » : PR #2753 SQUASH-MERGÉE** (2026-07-17 12:21Z). Demo
  `point-and-ask` (50e demo du catalogue, catégorie AR) : tap → capture frame ARCore
  (`Frame.cameraImage()`/`toArgbBitmap` existants) → **Gemini Nano on-device** via ML Kit
  GenAI Prompt API (`com.google.mlkit:genai-prompt:1.0.0-beta3`, API vérifiée par javap sur
  l'AAR) → carte réponse overlay. Gating AICore honnête (bannière UNAVAILABLE / CTA download
  avec progression MB), seam `AskEngine` (réel + `CannedAskEngine` déterministe sous qa_mode),
  timeout de capture 2 niveaux (qa_mode → frame synthétique ; sinon carte Failed), fermeture
  Image garantie même sur cancellation (UNDISPATCHED + try/finally), `playbackDataset` branché,
  flow Maestro ar.yaml, changelog fragment, llms.txt + gpt/knowledge régénérés, compteurs
  49→50 (17+33) sur 6 surfaces doc, off-by-one ar.yaml corrigé (29).
  Process : design doc (`.claude/plans/point-and-ask-2648.md`) → phases sur l'issue →
  review-fanout 4/4 **MERGE_AFTER_WARNINGS 0 ERROR** (7 warnings fixés in-PR) → QA émulateur
  PASSED (texte carte prouvé par uiautomator dump, pid stable, 0 FATAL) → CI verte → merge.
  Vérif : `gh pr view 2753 --json state` · `gh issue view 2648 --json comments`
- **#2754 OUVERTE + diagnostiquée à fond : ARCore n'a AUCUN build émulateur arm64.**
  Chaîne causale complète en commentaires d'issue, chaque étape testée sur le host :
  APK ARCore *device* exige la caméra id « 0 » or les AVD arm64 enrôlent back=« 10 »/front=« 1 »
  (identique emulator 35.6.11/36.5.11, image android-36/android-34, snapshot/cold boot) ;
  l'APK `_x86_for_emulator` crashe sur arm64 (`libarcore_c.so not found`). **La QA AR
  live-camera n'a JAMAIS marché sur le rig arm64** — masquée par leg advisory (#1651),
  replay (#1645) et environnements HDR pris pour un feed caméra. Voies : Rosetta x86_64
  (~10 GB, memory `feedback_arcore_emulator_mac_dead_end`) ou device physique.
  Vérif : `gh issue view 2754 --json comments`
- **Housekeeping host** : pin émulateur **35.6.11 re-posé** (le 36.5.11 du 28 mai cassait
  screenrecord ; gardé en `~/Library/Android/sdk/emulator-36.5.11-bak`) · AVD scratch +
  image android-34 supprimés · disque 5→17 Gi (purge clones review PRs mergées + caches) ·
  memory `project_emulator_36x_screenrecord_regression.md` réécrite avec le diagnostic.

## EN VOL / GATED

- **Rien en vol sur cette voie.** Claim 2648 libéré (`claim.sh --check 2648` = not claimed).
- **#2648 P2 (ancrage world-space AnchorNode+ViewNode) : GATED** — QA visuelle impossible
  sans caméra AR (cf. #2754). Débloquer via : (a) provisioning Rosetta x86_64 opt-in, ou
  (b) passe device physique Thomas (Pixel 8+ pour tester AUSSI le vrai Gemini Nano — le
  chemin réel n'a été exercé que par le canned engine). P3 (question libre, streaming,
  recipe llms.txt + skill) après P2.
- **#2754 côté harness = candidat prochain chantier** : warning honnête arm64 dans
  `setup-ar-emulator.sh`, probe topologie caméra dans `--check`, éventuel chemin Rosetta.

## Pièges reconduits

- Émulateur : un `adb reboot` ne ressuscite PAS un HAL caméra qemu wedgé (kill qemu + reboot
  frais nécessaire) ; le serveur adb peut afficher `offline` → `adb kill-server` d'abord.
- Contention : une session concurrente a lancé `DemoHostActivity` par-dessus ma QA (même
  package) — preuve par pid + `dumpsys activity` avant d'accuser son propre code
  (memory `feedback_emulator_apk_contention_qa`).
- `gpt/knowledge-*.md` : la CI `repo-hygiene` exige leur régénération après tout changement
  de llms.txt → `node tools/generate-gpt-knowledge.js` (arrivé sur main 07-17, absent des
  worktrees plus vieux — merger main d'abord).
