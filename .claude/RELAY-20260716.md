# RELAY 2026-07-16 — voie nice-mirzakhani « pleine balle » (post-handoff Fable)

> Suite de [RELAY-20260712.md](RELAY-20260712.md). Session relancée par Thomas
> (« Continue, pleine balle ») après le handoff de fenêtre — quota Fable encore actif.

## FAIT aujourd'hui (vérifiable par commande)

- **Stores 4.22.0 : propagation TERMINÉE** — Maven Central HTTP 200 (sceneview +
  arsceneview), npm sceneview-web@4.22.0. iOS live = 4.18.0, 4.22.0 en App Review.
  STATE.md bullets corrigés (l'ancienne ligne « Maven 404 » et « accord Apple expiré »
  étaient périmées). La consigne vigorous (« inspecter le run Release si 404 à T+2h »)
  est SOLDÉE — rien à inspecter.
- **#2697 CLOS** — PR #2712 squash-mergée : les 11 outils gateway hors tier map sont
  explicitement PRO_TOOLS (38), PRO_UPGRADE_MESSAGE 5 verticals/35 tools, mcpize 38 Pro,
  assertion inverse activée dans le truth test gateway. Claim libéré.
  Vérif : `gh pr view 2712 -R sceneview/sceneview --json state`
- **#2482 : plus AUCUN travail code** — item 2 (« Offline model ») était déjà livré ;
  item 3 (l10n gesture pill + « Aim at a surface… » → resources) = **PR #2717**
  (auto-merge armé, compile vérifiée). Claim libéré. Reste UNIQUEMENT la checklist
  Pixel 9 5 points (~3 min, device réel = Thomas) — détail en commentaire d'issue.
  Vérif : `gh pr view 2717 --json state` · `gh issue view 2482 --json comments`
- **Sécurité : PR #2719 (auto-merge armé)** — CVE-2026-48779 / GHSA-96hv-2xvq-fx4p
  (`ws` DoS) soldée sur les 4 alerts Dependabot HIGH via overrides scopés par ligne de
  version (gateway+telemetry → 8.21.1 ; RN transitive 7.x → 7.5.12, 6.x → 6.2.5, zéro
  bump de major). Tests verts avec ws patché (gateway 187, telemetry 58).
  Vérif : `gh pr view 2719 --json state` · `gh api repos/sceneview/sceneview/dependabot/alerts?state=open`
- **Runtime simulateur iOS RESTAURÉ** — iOS 26.3.1 (23D8133) Ready, 7.8G, Xcode 26.3.
  Les chantiers iOS sim locaux sont DÉBLOQUÉS (le blocage « 0 runtime » de STATE.md est levé).
  Vérif : `xcrun simctl runtime list`
- Bump v4.22.0 mergé sur main pendant le handoff (`760e1ecb0`) — gradle.properties = 4.22.0.

## EN VOL (cette voie)

- **#2700 (port iOS Explore multi-source) : CLAIMÉ, worker Opus dédié lancé 2026-07-16
  ~15h.** Mandat : parité #2685 (ModelSource Sketchfab|Icosa|PolyHaven) dans
  samples/ios-demo, clone lean /tmp/sv-2700, compile xcodebuild + QA visuelle sim
  (runtime restauré), PR « Closes #2700 » SANS auto-merge (MEDIUM+ → review-fanout
  orchestrateur obligatoire avant merge), DRAFT honnête si bloqué. **Ne pas dupliquer.**
  Vérif : `gh pr list -R sceneview/sceneview --search "2700"` ·
  `bash .claude/scripts/claim.sh --check 2700`
  → Si cette session meurt avant le review-fanout : un successeur lance `review-fanout`
  sur la PR du worker puis merge/renvoie, et libère le claim 2700.

## Constats / pièges reconduits

- Dependabot restants : **#2605** (detekt alpha.5) — son propre bump CASSE Lint+CI Gate,
  c'est un chantier d'accompagnement, pas un merge · **#2602** (compose-bom) — CI verte
  mais lot explicitement gated Thomas (STATE.md « à trancher »). Les 4 vulns HIGH, elles,
  sont traitées par #2719.
- #2698 (redeploy worker gateway, 3 tools de retard) reste MANUEL gated Thomas.
- #2540 (Desktop offscreen) : en attente du contributeur externe (réponse publique = Thomas).
- Émulateur/gradle : slot unique respecté (compile 2482 sérialisée derrière la session sv-2692).

---

# RELAY 2026-07-16 (append) — voie strange-goldberg : plan post-Fable Phase 1-2 INTÉGRALEMENT DÉROULÉ

> Reprise de la session strange-goldberg (résumé+plan Fable → exécution). Zéro item en vol
> sur ces voies. Aucune collision avec la voie nice-mirzakhani ci-dessus (vérifié).

## FAIT (vérifiable par commande)

### Phase 1 — audit threading/lifecycle #2668 : CLOSE
- **#2678 + #2686 + #2687 MERGÉS** après gate device-QA : leg web PASSED (BLOCKING, seul leg
  requis #1651) + QA manuelle `lighting-lab` émulateur (zéro crash JNI, HDR chargé) — les
  verdicts review Opus étaient déjà gravés en commentaires PR.
- **#2691 → PR #2710 MERGÉ** (guard `destroyed` loadModel web + settleLoad, review indép.).
- **#2688 → PR #2711 MERGÉ** (RerunBridgeTest borné soTimeout wire-level, test-only, 6/6 en 2.4s).
- Umbrella **#2668 + 5 sous-issues CLOSED**, claims relâchés.
  Vérif : `for p in 2678 2686 2687 2710 2711; do gh pr view $p --json state -q .state; done`

### Phase 2 — #2676/#2581 ombre Sketchfab : CLOSE
- QA différentielle run3 : baseline = viewport noir opaque zéro ombre ; after = sol clair +
  ombre contact **−73% luminance** (mesure intra-frame anti-drift d'orbite). Verdict posté PR.
- **Piège résolu (07-16)** : PR DIRTY sur conflit modify/rename (#2685 a renommé
  `SketchfabModelViewerScreen` → `GalleryModelViewerScreen`) — merge de main dans la branche
  (suivi rename git, compile vérifiée, zéro force-push) → **#2676 MERGÉ, #2581 CLOSED**.

### Phase 2 — #2646 SplatNode P1c : LIVRÉ, P1 (a+b+c) COMPLET
- **PR #2720 MERGÉ** : demo `splat-preview` (pattern GeometryDemo) + asset `rainbow_sphere.ply`
  440 KB **byte-reproductible** (`tools/generate-splat-sphere.py`, cmp clean).
- **Gate blending intra-batch (review #2681 W1) = STABLE** : 78 frames d'orbite, 0 spike/77
  paires à 8k splats. Mitigations RGBA32F/blendOrder documentées NON nécessaires à cette échelle.
- Review indép. MERGE OK (0 ERROR/0 WARNING). **Claim #2646 relâché**, epic ouverte
  (suite : ports web/iOS, dynamic splats, large-scan streaming).

## Piège harness (chip filé, non corrigé)
- device-QA android : `install -r` par-dessus un résidu → `No activity found` → flow catalog
  FAIL en 34s. Workaround prouvé : `uninstall` + `install -r -g` + vérif
  `resolve-activity --brief`. À durcir dans `qa-android-demos.sh`.

## Prochain gros chantier suggéré
- **#2648 "Point & Ask" Gemini Nano** (NEXT n°2, non claimé au moment de l'append) — session
  fraîche dédiée recommandée. Re-vérifier le claim avant de prendre :
  `bash .claude/scripts/claim.sh --check 2648`.

## MAJ ~16h — voie #2700 BOUCLÉE (fanout passé, auto-merge armé, claim libéré)

- Worker Opus : PR #2721 ready (build+20 tests+QA visuelle sim réelle Icosa/PolyHaven,
  gap RealityKit/USDZ documenté honnêtement, "coming soon" pour les catalogues glTF).
- **Review-fanout 4/4 : MERGE_AFTER_WARNINGS, 0 ERROR, breakingApi=false.** Les 6 warnings
  fixés in-PR (`80dcc2b`) : vrai streaming bound 32 MB (URLSession.bytes), 2 warnings de
  parité RÉFUTÉS par vérification du code Android (reset-on-switch + Trending→featured()
  sont la parité stricte — commentaires corrigés, comportement intact), init redondant,
  doc sync (samples.md 3-tab→4-tab, recipe #35 skill iOS, CLAUDE.md table). Re-test après
  fixes : TEST SUCCEEDED 20/20 sim 26.3.1.
- **Auto-merge armé** ; claim 2700 LIBÉRÉ (l'issue se ferme au merge via Closes).
  Vérif : `gh pr view 2721 -R sceneview/sceneview --json state,autoMergeRequest`
- Follow-up possible (non tracké, à trancher) : parité Explore multi-source côté web-demo.
- Cette voie n'a PLUS RIEN en vol. PRs armées restantes de la journée : #2717, #2719, #2721.

## MAJ 2026-07-17 — voie nice-mirzakhani : essaim shortlist BOUCLÉ (3/3), zéro en vol

- **#2724 CLOS** (PR #2729 mergée) : gpt/ régénéré depuis llms.txt + gate CI bloquant +
  câblage sync-versions --fix (piège version-bump fermé, preuve différentielle).
- **#2723 CLOS** (PR #2730 mergée) : binary-compatibility-validator, dumps .api committés,
  apiCheck bloquant en CI + pre-push-check [11/11] + Quality gate n°9.
- **#2722 : PR #2751 auto-merge armé, claim libéré.** Le worker est mort à la session-limit
  mi-QA ; la récupération orchestrateur a trouvé : (1) `site/js/model-sources.js` —
  TOUTE la couche catalogue — invisible-untracked sous la règle gitignore `site/`
  (force-add + note bruyante dans .gitignore : toujours `git add -f` après édition) ;
  (2) bug live-only : les miroirs Poly legacy sur web.archive.org (404 sans CORS)
  gagnaient la sélection de format → aucun modèle Icosa ne chargeait en vrai navigateur
  (les mocks ne pouvaient pas le voir). Fanout 4/4 MERGE_AFTER_WARNINGS → 7 warnings
  fixés (`4ff3ca8`), dont un résultat négatif intéressant : la réécriture blob-URI
  suggérée par le reviewer ÉCHOUE côté WASM (« Unable to load resources ») — le
  pré-peuplement Filament.assets (base = document.location, filament.js:947) est le
  seul mécanisme qui marche, désormais avec purge des clés par modèle.
  QA visuelle live : recherche Icosa 24 résultats, « Pond » (CC BY) téléchargé du
  storage Icosa et RENDU in-app (path bundle glTF), dégradation honnête sur asset
  Tilt Brush non-parsable. model-sources.spec.ts 9/9.
  Vérif : `gh pr view 2751 --json state` · échec pré-existant catalog.spec.ts:97
  différentiel-prouvé identique sur baseline main (GPU software local) — leg web CI = juge.
- Bilan session complète : PRs mergées #2696 #2712 #2717 #2719 #2721 #2729 #2730 (+#2751
  armée) · CVE ws soldée · runtime sim iOS restauré · bump 4.22.0 atterri sur main.
