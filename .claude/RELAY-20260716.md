# RELAY 2026-07-16 — voie nice-mirzakhani « pleine balle » (post-handoff Fable)

> Suite de [RELAY-20260712.md](RELAY-20260712.md). Session relancée par Thomas
> (« Continue, pleine balle ») après le handoff de fenêtre — quota Fable encore actif.

## FAIT aujourd'hui (vérifiable par commande)

- **Stores 4.22.0 : propagation TERMINÉE** — Maven Central HTTP 200 (sceneview +
  arsceneview), npm sceneview-web@4.22.0. iOS live = 4.18.0, 4.22.0 en App Review.
  STATE.md bullets corrigés (l'ancienne ligne « Maven 404 » et « accord Apple expiré »
  étaient périmées). La consigne vigorous (« inspecter le run Release si 404 à T+2h »)
  est SOLDÉE — rien à inspecter.
- **#2697 CLOS** — PR #2712 squash-mergée : les 11 outils gateway hors tier map sont
  explicitement PRO_TOOLS (38), PRO_UPGRADE_MESSAGE 5 verticals/35 tools, mcpize 38 Pro,
  assertion inverse activée dans le truth test gateway. Claim libéré.
  Vérif : `gh pr view 2712 -R sceneview/sceneview --json state`
- **#2482 : plus AUCUN travail code** — item 2 (« Offline model ») était déjà livré ;
  item 3 (l10n gesture pill + « Aim at a surface… » → resources) = **PR #2717**
  (auto-merge armé, compile vérifiée). Claim libéré. Reste UNIQUEMENT la checklist
  Pixel 9 5 points (~3 min, device réel = Thomas) — détail en commentaire d'issue.
  Vérif : `gh pr view 2717 --json state` · `gh issue view 2482 --json comments`
- **Sécurité : PR #2719 (auto-merge armé)** — CVE-2026-48779 / GHSA-96hv-2xvq-fx4p
  (`ws` DoS) soldée sur les 4 alerts Dependabot HIGH via overrides scopés par ligne de
  version (gateway+telemetry → 8.21.1 ; RN transitive 7.x → 7.5.12, 6.x → 6.2.5, zéro
  bump de major). Tests verts avec ws patché (gateway 187, telemetry 58).
  Vérif : `gh pr view 2719 --json state` · `gh api repos/sceneview/sceneview/dependabot/alerts?state=open`
- **Runtime simulateur iOS RESTAURÉ** — iOS 26.3.1 (23D8133) Ready, 7.8G, Xcode 26.3.
  Les chantiers iOS sim locaux sont DÉBLOQUÉS (le blocage « 0 runtime » de STATE.md est levé).
  Vérif : `xcrun simctl runtime list`
- Bump v4.22.0 mergé sur main pendant le handoff (`760e1ecb0`) — gradle.properties = 4.22.0.

## EN VOL (cette voie)

- **#2700 (port iOS Explore multi-source) : CLAIMÉ, worker Opus dédié lancé 2026-07-16
  ~15h.** Mandat : parité #2685 (ModelSource Sketchfab|Icosa|PolyHaven) dans
  samples/ios-demo, clone lean /tmp/sv-2700, compile xcodebuild + QA visuelle sim
  (runtime restauré), PR « Closes #2700 » SANS auto-merge (MEDIUM+ → review-fanout
  orchestrateur obligatoire avant merge), DRAFT honnête si bloqué. **Ne pas dupliquer.**
  Vérif : `gh pr list -R sceneview/sceneview --search "2700"` ·
  `bash .claude/scripts/claim.sh --check 2700`
  → Si cette session meurt avant le review-fanout : un successeur lance `review-fanout`
  sur la PR du worker puis merge/renvoie, et libère le claim 2700.

## Constats / pièges reconduits

- Dependabot restants : **#2605** (detekt alpha.5) — son propre bump CASSE Lint+CI Gate,
  c'est un chantier d'accompagnement, pas un merge · **#2602** (compose-bom) — CI verte
  mais lot explicitement gated Thomas (STATE.md « à trancher »). Les 4 vulns HIGH, elles,
  sont traitées par #2719.
- #2698 (redeploy worker gateway, 3 tools de retard) reste MANUEL gated Thomas.
- #2540 (Desktop offscreen) : en attente du contributeur externe (réponse publique = Thomas).
- Émulateur/gradle : slot unique respecté (compile 2482 sérialisée derrière la session sv-2692).
