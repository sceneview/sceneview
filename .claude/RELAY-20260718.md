# RELAY — 2026-07-18 — Fenêtre SHIP-READY (checkpoint de visibilité publique imminent)

> Écrit par la session kind-varahamihira (mandat direct Thomas 2026-07-18).
> À lire par TOUTES les sessions en vol avant tout merge touchant une surface user-facing.

## Mandat

Fenêtre de visibilité publique imminente : le repo, le site sceneview.github.io et les
apps stores vont recevoir un pic de trafic externe dans les prochains jours. Objectif :
**toutes les surfaces user-facing nickel** au moment où les gens cliquent.

## État vérifié (probes 2026-07-18 matin)

- **iOS 4.22.0 LIVE sur l'App Store** — approuvée par App Review le 07-17 (submission
  54e8b6da), released automatiquement. Vérif : `curl -s "https://itunes.apple.com/lookup?id=6761329763" | grep '"version"'` → 4.22.0.
- Play Store / Maven Central / npm `sceneview-web` 4.22.0 / website : déjà alignés
  (probes 07-17 + `npm view sceneview-web version` 07-18). `sceneview-mcp` 4.0.14 (track indépendant).
- ~~**README : badge Open Collective (l.24) + section « MCP Pro » (l.~501-509) ENCORE PRÉSENTS ;
  nav « Pricing » encore sur le site.**~~ **✅ FAIT (session dédiée sleepy-easley, commit `2f5cd1cae`,
  validation Thomas + push main 2026-07-18).** Purge Open Collective / MCP Pro / pricing-workers.dev
  sur 15 fichiers + `.github/FUNDING.yml` (bouton Sponsor GitHub) ; docs.yml a redéployé le site.
  Vérif live : `curl -s https://sceneview.github.io/ | grep -ci pricing` → **0**, idem `open collective`/`polar` → 0.
  Sponsor CTA = github.com/sponsors/sceneview partout. Laissé volontairement : code produit `mcp/src`
  + `mcp-gateway` (chemin d'upgrade fonctionnel), docs légales `mcp/PRIVACY.md`/`TERMS.md`, redirects
  `/go/*` (déjà vers GitHub Sponsors), CHANGELOG (historique).

- **SplatNode web (#2646 P2) MERGÉ** (PR #2771 → `main` `fb9d3303b`, 2026-07-18). La branche
  d'origine `claude/2646-splatnode-web` (e0c494050, 36 commits behind) a été reconciliée sur main
  (cherry-pick, seul conflit `libs.versions.toml` : gardé Android `filament=1.72.1` + ajout web
  `filamentWeb=1.52.3`). Review 4 agents indépendants = tous PASS ; CI 12/12 vert dont le smoke-test
  navigateur bloquant (`splat-bundle.spec.ts` → le cloud rend + blend stable sur orbit). Surfaces
  générées re-synchronisées (`sceneview-web.d.ts`, `gpt/knowledge`, énumérations skill web).
  ⚠️ La branche d'origine `claude/2646-splatnode-web` est maintenant redondante (son commit unique est
  dans le squash) mais CONSERVÉE — à Thomas de la supprimer s'il le souhaite (squash → SHA différent,
  le GC auto ne la verra pas forcément mergée).
- **Web-demo découvrabilité — RÉSOLU** (commit `40f436693`, GO Thomas). La demo web interactive
  `sceneview.github.io/web-demo/` (live, 9 onglets, multi-source, testée QA Playwright) n'était linkée
  NULLE PART : la carte « Web Demo » de la home pointait par erreur vers `/playground.html` (page Rerun).
  Fix : carte recâblée vers `/web-demo/` (nouvel onglet) + lien nav « Web Demo » ajouté aux 9 pages
  `website-static/`. Bonus : compte demos Android périmé corrigé (49/17+32 → 51/18+33, #2767). QA
  visuelle nav OK. docs.yml redéploie la home.
- **Audit fiches store — versions + textes OK, 1 finding VISUEL** (2026-07-18). Versions : iOS App
  Store **4.22.0 LIVE** (⚠️ `itunes.apple.com/lookup` SANS `country=` renvoie un storefront caché à
  4.18.0 — TOUJOURS passer `&country=us|fr|gb` → 4.22.0), Maven/npm 4.22.0, sceneview-mcp 4.0.14 (track
  indépendant). Descriptions iOS+Android : pro, evergreen, zéro monétisation, liens corrects.
  **Screenshots+icônes vérifiés VISUELLEMENT** (repo + pages store live) :
  - Play Store screenshots = les 4 du repo (Model Viewer/Dynamic Sky/Multiple Models/Lighting, helmet). OK.
  - App Store iOS screenshots présents mais BASIQUES (sphère chrome, sphère+occluder, primitives, catalogue
    Samples) — moins vendeurs que le helmet Android ; **ni iOS ni Android ne montrent l'AR** (sous-titre
    « AR Explorer » non illustré). Amélioration possible, non bloquant.
  - ⚠️⚠️ ~~**INCOHÉRENCE ICÔNE (action Thomas — Play Console gated)**~~ **→ DIAGNOSTIC FAUX, voir la
    correction en fin de fichier (2026-07-20) : ce n'est PAS une action manuelle, c'est le bug #2794.**
    L'énoncé du symptôme ci-dessous reste exact ; seule la cause attribuée était fausse. Le listing
    **Play Store live affiche une ANCIENNE icône VIOLETTE** (cube blanc/fond violet, sans crochets AR),
    alors que iOS App Store + l'app Android in-app (adaptive `#0D2137` + legacy) + l'asset repo
    `samples/android-demo/distribution/play-store/en-GB/graphics/icon-512.png` sont TOUS la nouvelle icône
    BLEUE (cube bleu + crochets AR). L'asset bleu correct est déjà commité — il n'a jamais été uploadé
    dans la Play Console. **Fix = uploader `graphics/icon-512.png` dans Play Console → Store listing → App
    icon.** Sans ça, branding incohérent iOS(bleu)/Play(violet) visible pendant le pic.

- **Sets de screenshots store capturés** (2026-07-18, émulateur Android + simulateur iOS) →
  `~/Desktop/sceneview-store-screenshots/` (android/ 6× 1080x2400 · ios/ 6× 1179x2556 · aperçus).
  Android via deep-link `am start --es demo <id>` + `screencap` ; iOS via le **launch arg `-demo <id>
  -qa_mode 1`** (PAS `simctl openurl` → SpringBoard affiche « Open in SceneView? » qui bloque ; le
  launch arg route sans dialogue — pipeline officiel `capture-appstore-screenshots.sh` +
  `samples/ios-demo/appstore-screenshots/README.md`). Upload = gated Thomas (Play Console / ASC).
- ⚠️ **Finding produit — parité VISUELLE iOS très en retrait d'Android.** Les demos iOS rendent sur
  fonds unis (noir/blanc), modèles petits/mal cadrés, **aucun environnement HDR** ; Android rend le
  helmet en HDR outdoor/indoor superbe. Les screenshots App Store « basiques » ne sont donc PAS un
  défaut de capture — c'est le rendu réel de l'app iOS. Améliorer la fiche App Store demande d'abord
  d'améliorer l'app (IBL/HDR + cadrage caméra). Aussi : `post-processing` affiche « This demo isn't
  available in the iOS app yet ». À relier à l'épopée parité #894 / audit `project_ios_parity_audit`.
- ⚠️ **Dimensions iOS** : capturé en 6.1" (iPhone 16, 1179x2556) ; l'App Store attend du **6.9"
  (1290x2796)** pour la soumission principale — aucun simulateur Pro Max n'existe sur ce host
  (à créer + recapturer avant upload ASC).

- ⚠️ ~~**À CONFIRMER — le Play Store semble servir 4.18.0, pas 4.22.0.**~~ **→ RÉFUTÉ (2026-07-20),
  voir la correction en fin de fichier. La prod Play est bien à jour ; le scraping HTML ci-dessous
  n'était pas une preuve et la conclusion qu'il a inspirée était fausse.** Raisonnement d'origine
  conservé ci-dessous pour mémoire : La page publique
  `play.google.com/store/apps/details?id=io.github.sceneview.demo` ne contient qu'un seul numéro de
  version applicatif : **4.18.0** (les autres sont des libs Google). Or iOS App Store = 4.22.0
  (`itunes lookup` **avec** `&country=us`), Maven Central = 4.22.0, npm `sceneview-web` = 4.22.0.
  **Réserve méthodo** : c'est du scraping HTML, pas une preuve — le service account Play de SceneView
  est un secret GitHub (`PLAY_STORE_SERVICE_ACCOUNT_JSON`), non disponible en local, donc l'API
  Play n'a pas pu trancher. **À vérifier dans la Play Console** (ou via un run de `play-store.yml`) :
  si la prod Android est bien restée en 4.18.0, les utilisateurs Android ont 4 versions de retard
  pendant le pic. Le RELAY affirmait « Play Store … déjà alignés » — cette affirmation n'a pas été
  reproduite ici.
- ✅ Fiche Play SceneView (formatage) : **propre** — aucun markdown ni HTML brut dans la description
  live (contrairement à AR Model Viewer, voir ci-dessous).

## Audit stores AR Model Viewer (repo séparé `~/Projects/ar-model-viewer`, branche `v2`)

Fait dans le cadre du mandat « stores clean partout » (2026-07-18). Détail complet dans le
`THOMAS-ACTIONS.md` de CE repo-là — résumé ici pour que les sessions SceneView ne re-cherchent pas :

- ✅ **CORRIGÉ + poussé** (`c555ddf` sur `v2`) : `full_description.txt` contenait 8 titres en `**gras**`
  markdown et `sync-play.py` envoie la description BRUTE → les astérisques s'affichaient
  littéralement sur la fiche publique. Réécrit en titres MAJUSCULES + emojis.
- 🔴 **Owner** : la fiche Play décrit la v2 (IA texte→3D, screenshot « Create with AI ») alors que le
  track production sert la **v1.3.0 legacy** ; la **v2.1.3 (code 29) est en `draft`**. Risque policy
  Play « Misrepresentation ». Publier la v2.1.3 ou replier la fiche.
- 🟠 **Owner** : fiche iOS auto-contradictoire sur l'IA (subtitle « AI Generator » vs description
  « COMING SOON » vs paywall « rolling out »), app iOS pas encore publiée. `aiEnabled=true` en prod
  mais dépend du solde Tripo.
- ⚠️ **Ce repo a une session concurrente active** (commits pendant l'audit) : j'ai sauvegardé mon
  travail sur la branche `store-audit-20260718` avant de rebaser, puis poussé en fast-forward.

## ⚠️ CORRECTION de mon propre constat : « rendu iOS pauvre » = mesuré sur SIMULATEUR

J'ai écrit plus haut que la parité visuelle iOS était « très en retrait d'Android ». **Cette
conclusion est fragile et je la corrige** : toutes mes captures iOS viennent du **simulateur**, et
RealityKit y dégrade connu­ment l'IBL / les réflexions / le skybox (pas de pipeline Metal complet).

Ce qui est **factuel** (lu dans le code, indépendant du rendu) :
- `showSkybox` vaut **`true` par défaut** pour tous les presets (`Environment.swift:33`), et le
  skybox EST implémenté sur iOS (`SceneView.swift:1315` → `loadedSkyboxResource`, appliqué via
  `content.environment`). Donc un fond HDR est censé s'afficher.
- Or mes captures simulateur montrent un **fond noir** → soit le simulateur ne rend pas le skybox,
  soit il y a un vrai bug. **Non tranché : il faudrait un iPhone physique** (Thomas n'a qu'un
  Pixel 9 ici).
- Ce qui reste vrai sans réserve : **17 des 26 demos non-AR n'avaient aucun `.environment()`** —
  ça, c'est un fait du code, pas du rendu. D'où les 3 fixes ci-dessous.

**À faire par quelqu'un avec un iPhone** : lancer `-demo materials` / `-demo scene-gallery` sur un
device réel et comparer. Si le fond HDR apparaît sur device, alors les screenshots App Store
« basiques » viennent d'une capture simulateur et il suffit de recapturer sur device. Si le fond
reste noir sur device, c'est un vrai bug de skybox iOS à ouvrir.

## ✅ Corrigé côté rendu iOS (3 demos, build vert)

`MaterialsDemo`, `SceneGalleryDemo`, `MultiModelDemo` n'avaient aucun IBL alors qu'elles chargent
des modèles PBR curatés — et `MaterialsDemo` démontre `KHR_materials_transmission` /
`_iridescence` / `_sheen`, qui n'existent que par ce qu'ils font à la lumière environnante. Ajout de
`.environment(.studio)` (le preset que `ModelViewerDemo` porte déjà depuis #2114). Commits
`c77404ea7` + `406095bea`. Les demos dont le fond neutre est le sujet (Light Types, Movable Light,
Fog) sont volontairement épargnées. 12/26 demos non-AR portent désormais un IBL.

## Demandes aux sessions en vol

1. **Main reste vert et démo-propre.** Merges risqués sur `samples/android-demo`,
   `website-static/`, `README.md` → repousser après le checkpoint, ou triptyque complet
   (reviews + tout-à-jour + QA visuelle) sans exception.
2. **#2239 (regroupement samples ~18 cartes) et #2024 P5** : si la session démarre, viser
   un état démo-propre à chaque merge intermédiaire ; sinon, laisser pour après le checkpoint.
3. **Zones com publiques** (badges/section Support du README, nav Pricing du site) :
   NE PAS toucher depuis les sessions repo (règle STATE.md l.24) — session dédiée only.

## Prochaine action exacte

- Session dédiée (hors repo) : cleanup com README + site, validation Thomas, puis vérif :
  `sed -n '24p' README.md` (plus d'Open Collective) et
  `curl -s https://sceneview.github.io/ | grep -ci pricing` → 0.
- Cette session (kind-varahamihira) : prépare le checkpoint de visibilité — pas de code repo.

## Pièges

- Ne pas ré-introduire : Polar, Open Collective, « MCP Pro », pricing/workers.dev,
  monétisation. Sponsor CTA = github.com/sponsors/sceneview uniquement.
- La nightly du 07-18 est la première à signal fiable (reporter #2743) — auto-issue après
  2 nuits rouges by design, ne pas ouvrir d'issue à la main.

---

## MAJ 2026-07-18 — wizardly-feynman (triage dependabot + GC branches)

### Fait
- Triage PRs dependabot : #2702 (play-services-location 21.4.0) + #2602 (compose-bom
  2026.06.01) MERGÉES (CI verte + compile locale `:sceneview:` / `:arsceneview:`) ;
  **#2605 FERMÉE** — detekt `2.0.0-alpha.5` fait échouer l'analyse sur du code inchangé
  (`:sceneview-core:detektCommonMainSourceSet` 12 issues, `:sceneview:detekt` 7 : nouveaux
  CyclomaticComplexMethod / LongMethod / NestedBlockDepth sur `LightEstimator.update`,
  `ARSessionFailure.from`…). L'adopter demande une baseline ou des refactors ciblés — PR
  dédiée, pas un bump auto. Vérif : `gh pr view 2605 --json state`.
- GC branches : 1 remote (`claude/sceneview-2540-review-e9868b`) + les locales mergées
  supprimées via `cleanup-branches-worktrees.sh --yes`. Aucun worktree prunable.

### ⚠️ CORRECTION d'une entrée FAUSSE des RELAY précédents — `claude/agitated-tereshkova-7a8fe1`
RELAY-20260712 § nice-mirzakhani la décrivait comme « **50 commits jamais poussés**, fix iOS
Sketchfab→USDZ #2252, ~6 semaines, **documentée nulle part ailleurs** » et demandait un audit
avant purge. **Les trois affirmations sont fausses ou périmées** :
- La branche **n'existe plus** (supprimée par une autre voie ; ce n'est PAS le GC de cette
  session — elle figurait encore dans la liste après le `--yes`).
- Son tip récupéré parmi les commits dangling (`git fsck`) = `2209d29a2` — **2 commits hors
  main, pas 50**.
- **Le travail EST dans main** : commit `6de79334d` « fix(stores): resolve iOS+macOS App
  Review rejections — USDZ load, app name, whatsNew, build 366 (#2252) » touche **les mêmes
  9 fichiers** + les mêmes fragments `changelog.d/2252-ios-sketchfab-usdz.md` et
  `2252-store-name-and-whatsnew.md`. Le code de main préfère bien l'USDZ
  (`SketchfabService.swift` : `appleFormat`, « USDZ is the only format we download »).
  ⇒ **RIEN N'A ÉTÉ PERDU** — la branche était un doublon obsolète du travail livré.
- Ref de sécurité conservée : `recovered/tereshkova-2252-ios-usdz` (locale). Supprimable
  (preuve ci-dessus) — laissée à Thomas.
- ⚠️ Note : l'issue **#2252 est en réalité « ci(app-store): macOS export blocked — Apple
  Distribution cert non-extractable », CLOSED le 2026-05-30**. Les commits la citent en
  référence mais traitent d'autre chose : ne pas déduire l'objet d'un ticket de son numéro
  dans un message de commit.

### ⚠️ Risque systémique — 14 branches n'existent QUE sur ce Mac
Mesuré ce jour (`git ls-remote --heads origin <b>` vide + commits hors `origin/main`) :
`claude/dreamy-tu-4fdc34` (2738), `feat-910-ios-occlusion-material` (3058),
`feat-2148-ios-environment-demo-rebased` (3048), `fix-2122-arconfig-materials-null` (3029),
`festive-kare-6f3344` (31), `quirky-hellman-e63555` (5), `2646-splatnode-web-merge` (4),
`2648-p3-ask-anything` (2), `jolly-hypatia-a232f7` (2), `nifty-cray-eb0bd0` (2),
`2648-point-and-ask-recipe`, `2754-arm64-camera-honesty`, `issue-batch-rigor`,
`sleepy-easley-0de057` (1 chacune).
Invisibles depuis GitHub : un GC un peu zélé ou un disque perdu les efface définitivement —
tereshkova est le premier cas concret. **Non poussées volontairement par cette session** :
`sceneview/sceneview` est PUBLIC, y publier 14 branches dont une part est du déchet
expérimental est un choix qui appartient à Thomas (si go : un seul `git push` multi-refs,
jamais 14 pushes — pattern bot).

### 🪤 Piège outillage — `.claude/settings.local.json` est TRACKÉ par git
Lancer un `rm -rf` via Bash fait persister la permission dans l'allowlist de ce fichier :
`"Bash(rm -rf -- * .git .github .claude .maestro)"` s'y est retrouvée écrite (purge d'un clone
mort). Une allowlist permanente de `rm -rf` avec glob, dans un fichier versionné, est un
footgun sérieux — retirée ici. **Vérifier `git diff .claude/settings.local.json` avant tout
commit** ; ne jamais laisser filer une entrée `rm -rf` / `git push --force` dans cette liste.

### Vérif de cette section
`gh pr view 2702 --json state` · `gh pr view 2605 --json state` ·
`git log origin/main --oneline --grep="USDZ"` · `git branch --list 'recovered/*'`

---

## MAJ 2026-07-20 — sleepy-easley : DEUX corrections de mes propres constats + la cause racine réelle

### ✅ RÉFUTÉ — « le Play Store sert 4.18.0 »

**C'était faux.** La prod Android est à jour. Preuve, qui remplace le scraping HTML :

```
gh run list --workflow=play-store.yml --limit 12   # 4.20.0, 4.21.0, 4.21.2, 4.22.0, 4.23.0 → success
gh run view 29641109721 --json jobs                # "Publish to production → success"
```

`play-store.yml` publie avec `status: completed` sur `production` pour un push de tag. Cinq releases
consécutives ont réussi. Ma « réserve méthodo » du 07-18 était juste sur la forme (scraping ≠ preuve)
mais j'ai quand même écrit la conclusion alarmante en tête de bullet — **exactement le biais
`feedback_prose_hardening_without_measurement`** : la réserve était en bas, l'affirmation en haut.
La bonne commande était accessible depuis le début et ne demandait aucun secret.

### ✅ RE-DIAGNOSTIQUÉ — l'icône violette n'est PAS une action manuelle Play Console

Le symptôme était juste, la cause attribuée était fausse. `icon-512.png` (bleu) **est** uploadé à
chaque release — puis annulé. Cause racine : `play_listing.py` déclarait deux `imageType` absents de
l'enum `AppImageType` de Play (`tabletScreenshots` / `tabletScreenshots10` au lieu de
`sevenInchScreenshots` / `tenInchScreenshots`). Le 400 sur le premier type tablette fait avorter
l'**edit atomique**, donc TOUT est rollbacké : icône, titre, descriptions, screenshots téléphone.

```
[listing] en-GB: icon (1 image)          ← part bien…
HTTPError: 400 ... /listings/en-GB/tabletScreenshots   ← …puis tout est annulé
```

Invisible parce que `sync-listing` est `continue-on-error` (le run reste vert), qu'il ne tourne que
sur un minor bump, et que rien ne relisait la fiche live. Cassé depuis #1710, devenu bloquant quand
les PNG tablette ont atterri. L'extraction en script (#2764) a repris le bug fidèlement.

→ Issue **#2794**, PR **#2795** (correction + garde-fou hors-ligne `unknown_image_types()` + job
`listing-drift` read-only quotidien dans `maintenance.yml`).

### ⚠️ À LIRE AVANT LA PROCHAINE RELEASE MINEURE

Le sync n'ayant **jamais** abouti, l'état actuel de la fiche live a été posé à la main et sa dérive
vis-à-vis du repo n'est **pas mesurée**. Le sync est *delete-then-upload* par imageType : la première
exécution réussie **remplacera** le texte et les images en ligne par ceux du repo. C'est la sémantique
voulue (store-as-code), mais il faut le voir venir → lire la sortie du job `listing-drift` avant de
tagger. Ce n'est pas un blocage, c'est une vérification de 30 s.

### Ce qui reste réellement gaté Thomas

- **AR Model Viewer** : déclaration FGS (+ vidéo 30-45 s) — gèle fiche + upload + promotion v2.1.3.
  L'app n'est pas installée sur le Pixel 9 : pousser l'AAB `vc29` d'abord.
- **iOS skybox** : trancher simulateur vs vrai bug demande un iPhone physique (cf. section 07-18).
- **Icône Play** : plus rien à faire à la main — elle se corrigera au premier sync réussi (post-#2795).

### Vérif de cette section
`gh pr view 2795 --json state` · `gh issue view 2794 --json state` ·
`bash .claude/scripts/test-store-sync.sh` · `gh run view 29641109721 --json jobs`

---

## 2026-07-21 — session `dreamy-lederberg` · ⚠️ DEMANDE DE FENÊTRE ÉMULATEUR EXCLUSIVE

### Le point dur : le rig Rosetta ne peut pas partager l'hôte

`setup-ar-emulator.sh --rosetta` (#2758) fait tourner un guest **x86_64 sous TCG**, donc **sans
aucune accélération matérielle** (Apple Silicon ne peut pas accélérer un guest x86). Conséquence
mesurée aujourd'hui sur ce Mac **M3 16 Gi** :

- rig lancé 16:03:06, un émulateur arm64 `Tablet10_QA` (2 Gi, autre session) lancé **16:03:11** ;
- 12 min plus tard : **~5 Gi de swap**, RSS des DEUX guests en *chute* (rig 819 → 424 Mo),
  `adb: offline` figé, zéro progression. Un guest TCG dont la mémoire part au swap n'avance plus.
- j'ai arrêté **mon** rig (le plus lourd, et le seul que je contrôle) plutôt que de faire thrasher
  l'hôte — ce qui aurait aussi pollué la QA de l'autre session.

Le gate RAM de `emulator-select.sh` **ne peut pas** empêcher ça : deux sessions qui mesurent la RAM
libre au même instant le passent toutes les deux. C'est une course inter-sessions, pas un bug.

### Ce que je demande

Une **fenêtre d'environ 1 h sans autre émulateur** pour retenter le boot du rig. Concrètement :
avant de booter un émulateur, `adb devices` ; si `emulator-5584` est là, c'est le rig — le laisser
finir (il est invisible au pool, il ne vous prendra jamais votre lease). Inversement je ne toucherai
jamais à `emulator-5554` : au moment où j'écris, une session y fait tourner `Tablet10_QA`, relancé
au moins une fois (PID différent), donc du travail actif.

### État de mon chantier (les deux PRs restent DRAFT, rien de non prouvé n'est revendiqué)

- **#2758 — PR #2793** : `--rosetta` + 2 correctifs issus des tentatives de boot.
  (a) **toute sonde guest est bornée en temps** : `adb shell getprop` peut bloquer indéfiniment
  pendant que `adb devices` affiche encore `device`, ce qui avait figé la boucle d'attente
  **au-delà de son propre timeout de 90 min** ; verdict « guest wedged » à 420 s. `EMU_ROSETTA_LOG`
  rend le log persistant (macOS vide `/tmp` au reboot — le log du run précédent a été perdu ainsi).
  (b) **port 5600 → 5584** : l'émulateur alerte à chaque boot `Requested adb port (5601) is outside
  the recommended range [5555,5586]`. 5584 est le dernier port console dans la fenêtre supportée.
  ⛔ **Vérité de fin NON ATTEINTE** : le rig n'a jamais fini de booter → camera id 0 + session
  ARCore réelle restent à prouver.
- **#2648 P2 — PR #2846** : ancrage world-space de la réponse Point & Ask.
  ⚠️ **Piège pour quiconque reprend une branche Point & Ask** : main a reçu **#2770 « film-mode »**
  (capture PixelCopy composée, props au long-press, `planeRenderer=false`). Une branche partie
  d'avant ce commit **écrase 639 lignes de main** si on copie les fichiers au lieu d'appliquer le
  diff. J'ai réimplémenté par-dessus film-mode.
  Review round 1 : **2 ERROR réelles corrigées** — (1) le yaw de la carte double-comptait
  l'orientation du hit pose ARCore (déjà « Z+ toward the user's device »), les cartes tournaient
  le dos dès qu'on bougeait ; (2) un round zombie survivant au Reset sortait le round SUIVANT de
  sa capture et **figeait la démo définitivement**. QA visuelle impossible sans session ARCore
  réelle (en `qa_mode`, aucun hit → l'ancrage n'est jamais exercé) → même blocage que #2758.

### Vérif de cette section
`gh pr view 2793 --json state,isDraft` · `gh pr view 2846 --json state,isDraft` ·
`adb devices` · `bash .claude/scripts/setup-ar-emulator.sh --check --rosetta`
