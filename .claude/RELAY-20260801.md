# RELAY — 2026-08-01

## ⚠️ Disclosure: I disturbed `emulator-5554` while a peer session was driving it (22:26–22:29 CEST)

**Who:** the session that merged #2945 (`9eb882f03`), then tried to close the #2942
device-QA gap on the shared QA emulator.

**What I did to `emulator-5554`, verbatim and timestamped:**

| Time (CEST) | Command | Effect on a peer's run |
|---|---|---|
| 22:25:17 | `setup-ar-emulator.sh` (no flag) | leased the already-running `Pixel_7a`, sticky session `sv-1785615917-52167` |
| 22:27 | `adb -s emulator-5554 install -r samples/android-demo/build/outputs/apk/debug/android-demo-debug.apk` | **replaced the installed `io.github.sceneview.demo`** with my local debug build |
| 22:27 | `adb -s emulator-5554 shell pm clear io.github.sceneview.demo` | **wiped the app's data/state** |
| ~22:28:1x | `adb shell am start -W -n io.github.sceneview.demo/.MainActivity --es demo materials` | navigated the app to the Materials demo |

**How I found out it was not mine to drive:** the lease file
`${TMPDIR}/sceneview-device-qa-emu/emulator-5554.lease` holds pid **55682**, written at
**22:26** — neither of my two tokens (`sv-1785615897-51491`, `sv-1785615917-52167`). A peer
took the reservation one minute after I did. Corroborating evidence: the app process
restarted at **22:28:46** without any command from me, and the next screenshot showed
**Contact Shadow Preview**, not the `materials` demo I had deep-linked.

**Consequences you may see:** the installed APK on `emulator-5554` is now my local
`assembleDebug` of `e75304b62` (not a CI artifact); app data was cleared at 22:27, so any
state your run had set up is gone; the app was force-navigated to `materials`.

**What I did NOT do** (deliberately, per `feedback_emulator_apk_contention_qa`): I did not
try to restore your state — I do not know your target state — and I did not release or
overwrite the lease that now holds pid 55682. I stopped driving the device the moment the
foreign lease was measured. Re-install your own APK before trusting anything on this
emulator.

## Repo facts verified in this session (useful to peers)

- `main` @ `e75304b62`. `.git/shallow` was removed by a peer at 21:15 (`.git/shallow.disabled`
  remains) — `merge-base` and three-dot diffs work again in the shared clone.
- `:samples:android-demo:assembleDebug` **fails from a poisoned Gradle build-cache entry** on
  this host: `Unresolved reference 'WallPlacementPhase'` in `WallPlacementDemo.kt`, while the
  enum is intact at `arsceneview/src/main/java/io/github/sceneview/ar/WallPlacement.kt:208-220`
  (all four entries, added whole by `efca2363c`). `arsceneview/build/tmp/kotlin-classes/debug`
  was **empty**. Deleting the module `build/` alone is not enough — the cache restores the same
  entry (build re-failed in 5 s, "11 from cache"). The documented remedy is what works:
  `rm -rf arsceneview/build samples/android-demo/build && ./gradlew … --no-build-cache`
  → `BUILD SUCCESSFUL`. See the `project_gradle_poisoned_cache_false_red` memory (#2938).
- #2942 (Materials chip round-trip) is still **open on purpose**: the source-level fix from
  #2939 + #2945 is verified present on `main` (`MaterialsDemo.kt:336-347`, `:809-810`), but the
  repro has never been executed on a device. Status posted as
  https://github.com/sceneview/sceneview/issues/2942#issuecomment-5153265012 — the device pass
  is what closes it, and it did **not** happen here.
