<?xml version='1.0' encoding='UTF-8'?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>ConstructorParameterNaming:ARSceneScope.kt:ARSceneScope$_nodes: SnapshotStateList&lt;NodeImpl></ID>
    <ID>CyclomaticComplexMethod:ARScene.kt:@Composable fun ARSceneView( modifier: Modifier = Modifier, /** * Selects whether the backing surface is SurfaceView-based ([SurfaceType.Surface], renders * behind Compose, best performance) or TextureView-based ([SurfaceType.TextureSurface], * renders inline, supports alpha blending). */ surfaceType: SurfaceType = SurfaceType.Surface, /** * Provide your own instance if you want to share Filament resources between multiple views. */ engine: Engine = rememberEngine(), /** * Consumes a blob of glTF 2.0 content (either JSON or GLB) and produces a [Model] object, * which is a bundle of Filament textures, vertex buffers, index buffers, etc. */ modelLoader: ModelLoader = rememberModelLoader(engine), /** * A Filament Material defines the visual appearance of an object. * Materials function as templates from which [MaterialInstance]s can be spawned. */ materialLoader: MaterialLoader = rememberMaterialLoader(engine), /** * Utility for decoding an HDR file or consuming KTX1 files and producing Filament textures, * IBLs, and sky boxes. */ environmentLoader: EnvironmentLoader = rememberEnvironmentLoader(engine), /** * Fundamental session features that can be requested. * @see Session.Feature */ sessionFeatures: Set&lt;Session.Feature> = setOf(), /** * Optional MP4 dataset [File] to play back instead of the live camera feed. * * When non-null, ARCore is configured for **playback** mode: the session re-runs the * recorded camera frames, IMU data, planes, anchors and depth from the dataset, exactly * as captured by a previous call to * [Session.startRecording][com.google.ar.core.Session.startRecording] (or via the * [io.github.sceneview.ar.recording.ARRecorder] helper). This is the standard ARCore * record-replay workflow — capture an outdoor session once, iterate at the desk against * the recording, share the MP4 with teammates to reproduce bugs deterministically. * * The file must be passed before the session resumes; SceneView wires it on session * creation. Switching between live and playback at runtime requires the [ARSceneView] * to be fully recreated (e.g. via Compose `key(playbackDataset) { … }`), because ARCore * binds the playback source to the [Session] for its entire lifetime. * * Default `null` (live camera mode). */ playbackDataset: File? = null, /** * Selects the camera config to use. The returned config must be one returned by * [Session.getSupportedCameraConfigs]. * * Defaults to [highestResolutionCameraConfig] so every AR scene — and in particular every * [ARRecorder] recording — runs at the device's full back-camera resolution rather than * ARCore's low-res 640×480 CPU-stream default ([#1065](https://github.com/sceneview/sceneview/issues/1065)). * Pass `null` to keep ARCore's stock default config, or supply a custom selector. */ sessionCameraConfig: ((Session) -> CameraConfig)? = ::highestResolutionCameraConfig, /** * Configures the session and verifies that the enabled features in the specified session * config are supported with the currently set camera config. */ sessionConfiguration: ((session: Session, Config) -> Unit)? = null, /** * Enable the plane renderer. */ planeRenderer: Boolean = true, /** * The [ARCameraStream] to render the camera texture. */ cameraStream: ARCameraStream? = rememberARCameraStream(materialLoader), /** * Encompasses all the state needed for rendering a [SceneView]. */ view: View = rememberARView(engine), /** * Controls whether the render target is opaque or not. Default `true`. */ isOpaque: Boolean = true, /** * A [Renderer] instance represents an operating system's window. */ renderer: Renderer = rememberRenderer(engine), /** * Provide your own instance if you want to share [Node]s' scene between multiple views. */ scene: Scene = rememberScene(engine), /** * Defines the lighting environment and the skybox of the scene. * * Defaults to [rememberAREnvironment] with an [EnvironmentLoader], which ships * the neutral IBL baseline so PBR materials reflect something sensible until * ARCore's `ENVIRONMENTAL_HDR` light estimate stabilises (#1063). */ environment: Environment = rememberAREnvironment(engine), /** * Always add a direct light source since it is required for shadowing. */ mainLightNode: LightNode? = rememberMainLightNode(engine), /** * Optional secondary "fill" directional light that softens the shadows produced by * [mainLightNode]. Default mirrors the 3D `Scene` two-light setup (main + fill at 30%) * shipped in v4.1.0 — AR scenes now match the 3D demo baseline (#1063). Pass `null` for * a single-light AR scene. ARCore's `ENVIRONMENTAL_HDR` estimate only drives * [mainLightNode]; [fillLightNode] keeps its baseline color/intensity each frame. */ fillLightNode: LightNode? = rememberFillLightNode(engine), cameraNode: ARCameraNode = rememberARCameraNode(engine), /** * Optional **absolute exposure scaling** applied to the AR camera. * * When non-null, this value is forwarded to Filament's * [io.github.sceneview.components.CameraComponent.setExposure] (single-`Float` * overload), which sets the aperture to 1.0, the shutter speed to 1.2 s, and * derives the sensitivity to match the requested **linear exposure value** * (1.0 ≈ ISO 100 ≈ EV 0). This is **not** a signed EV-stop bias — negative * values clamp the framebuffer to zero (full black, see #1179). Realistic * values land between ~0.05 (very dark) and ~16 (very bright). * * In practice, prefer leaving this `null`. The default exposure set by * [ARDefaultCameraNode] (f/12, 1/200 s, ISO 200 ≈ EV 11.6, after #1088) * matches the v4.1.0 main+fill light setup (10k + 3k lux) and is correct * for both back- and front-camera sessions on every device tested through * Pixel 9. Override only when porting a legacy AR scene that depends on * the linear-gain form. * * To darken / brighten by a number of stops instead, build a fresh * [ARCameraNode] and call the 3-arg `setExposure(aperture, shutter, ISO)` * on it (see [ARDefaultCameraNode] for the parity-correct values). */ cameraExposure: Float? = null, /** * Physics system to handle collision between nodes, hit testing on nodes, etc. */ collisionSystem: CollisionSystem = rememberCollisionSystem(view), /** * Used for [io.github.sceneview.node.ViewNode]s that can display an Android [android.view.View]. */ viewNodeWindowManager: WindowManager? = null, /** * The session is ready to be accessed. */ onSessionCreated: ((session: Session) -> Unit)? = null, /** * The session has been resumed. */ onSessionResumed: ((session: Session) -> Unit)? = null, /** * The session has been paused. */ onSessionPaused: ((session: Session) -> Unit)? = null, /** * Invoked when an ARCore error occurred during session creation / resume / configuration * (missing ARCore install, permission denied, device-unsupported, etc.). * * Playback-dataset failures (`PlaybackFailedException` or any exception thrown by * [Session.setPlaybackDataset]) are routed here ONLY when [onPlaybackFailed] is `null`. * Set [onPlaybackFailed] for fine-grained handling of "bad MP4 path" vs "AR unavailable". */ onSessionFailed: ((exception: Exception) -> Unit)? = null, /** * Optional dedicated callback for failures that originate from the * [playbackDataset] binding — typically `PlaybackFailedException` from ARCore when the * MP4 cannot be opened, parsed, or already has an active recording. * * When `null` (default), playback failures fall through to [onSessionFailed] for * backwards compatibility. */ onPlaybackFailed: ((exception: Exception) -> Unit)? = null, /** * Updates of the state of the ARCore system. * Invoked once per [Frame] immediately before the Scene is updated. */ onSessionUpdated: ((session: Session, frame: Frame) -> Unit)? = null, /** * Listen for camera tracking failure. */ onTrackingFailureChanged: ((trackingFailureReason: TrackingFailureReason?) -> Unit)? = null, /** * The listener invoked for all the gesture detector callbacks. */ onGestureListener: GestureDetector.OnGestureListener? = rememberOnGestureListener(), onTouchEvent: ((e: MotionEvent, hitResult: HitResult?) -> Boolean)? = null, permissionHandler: ARPermissionHandler? = (LocalContext.current as? androidx.activity.ComponentActivity)?.let { activity -> remember(activity) { ActivityARPermissionHandler(activity) } }, lifecycle: Lifecycle = LocalLifecycleOwner.current.lifecycle, /** * DSL block for declaring AR nodes via [ARSceneScope]. */ content: (@Composable ARSceneScope.() -> Unit)? = null )</ID>
    <ID>CyclomaticComplexMethod:LightEstimator.kt:LightEstimator$fun update(session: Session, frame: Frame, camera: Camera): Estimation?</ID>
    <ID>LongMethod:ARScene.kt:@Composable fun ARSceneView( modifier: Modifier = Modifier, /** * Selects whether the backing surface is SurfaceView-based ([SurfaceType.Surface], renders * behind Compose, best performance) or TextureView-based ([SurfaceType.TextureSurface], * renders inline, supports alpha blending). */ surfaceType: SurfaceType = SurfaceType.Surface, /** * Provide your own instance if you want to share Filament resources between multiple views. */ engine: Engine = rememberEngine(), /** * Consumes a blob of glTF 2.0 content (either JSON or GLB) and produces a [Model] object, * which is a bundle of Filament textures, vertex buffers, index buffers, etc. */ modelLoader: ModelLoader = rememberModelLoader(engine), /** * A Filament Material defines the visual appearance of an object. * Materials function as templates from which [MaterialInstance]s can be spawned. */ materialLoader: MaterialLoader = rememberMaterialLoader(engine), /** * Utility for decoding an HDR file or consuming KTX1 files and producing Filament textures, * IBLs, and sky boxes. */ environmentLoader: EnvironmentLoader = rememberEnvironmentLoader(engine), /** * Fundamental session features that can be requested. * @see Session.Feature */ sessionFeatures: Set&lt;Session.Feature> = setOf(), /** * Optional MP4 dataset [File] to play back instead of the live camera feed. * * When non-null, ARCore is configured for **playback** mode: the session re-runs the * recorded camera frames, IMU data, planes, anchors and depth from the dataset, exactly * as captured by a previous call to * [Session.startRecording][com.google.ar.core.Session.startRecording] (or via the * [io.github.sceneview.ar.recording.ARRecorder] helper). This is the standard ARCore * record-replay workflow — capture an outdoor session once, iterate at the desk against * the recording, share the MP4 with teammates to reproduce bugs deterministically. * * The file must be passed before the session resumes; SceneView wires it on session * creation. Switching between live and playback at runtime requires the [ARSceneView] * to be fully recreated (e.g. via Compose `key(playbackDataset) { … }`), because ARCore * binds the playback source to the [Session] for its entire lifetime. * * Default `null` (live camera mode). */ playbackDataset: File? = null, /** * Selects the camera config to use. The returned config must be one returned by * [Session.getSupportedCameraConfigs]. * * Defaults to [highestResolutionCameraConfig] so every AR scene — and in particular every * [ARRecorder] recording — runs at the device's full back-camera resolution rather than * ARCore's low-res 640×480 CPU-stream default ([#1065](https://github.com/sceneview/sceneview/issues/1065)). * Pass `null` to keep ARCore's stock default config, or supply a custom selector. */ sessionCameraConfig: ((Session) -> CameraConfig)? = ::highestResolutionCameraConfig, /** * Configures the session and verifies that the enabled features in the specified session * config are supported with the currently set camera config. */ sessionConfiguration: ((session: Session, Config) -> Unit)? = null, /** * Enable the plane renderer. */ planeRenderer: Boolean = true, /** * The [ARCameraStream] to render the camera texture. */ cameraStream: ARCameraStream? = rememberARCameraStream(materialLoader), /** * Encompasses all the state needed for rendering a [SceneView]. */ view: View = rememberARView(engine), /** * Controls whether the render target is opaque or not. Default `true`. */ isOpaque: Boolean = true, /** * A [Renderer] instance represents an operating system's window. */ renderer: Renderer = rememberRenderer(engine), /** * Provide your own instance if you want to share [Node]s' scene between multiple views. */ scene: Scene = rememberScene(engine), /** * Defines the lighting environment and the skybox of the scene. * * Defaults to [rememberAREnvironment] with an [EnvironmentLoader], which ships * the neutral IBL baseline so PBR materials reflect something sensible until * ARCore's `ENVIRONMENTAL_HDR` light estimate stabilises (#1063). */ environment: Environment = rememberAREnvironment(engine), /** * Always add a direct light source since it is required for shadowing. */ mainLightNode: LightNode? = rememberMainLightNode(engine), /** * Optional secondary "fill" directional light that softens the shadows produced by * [mainLightNode]. Default mirrors the 3D `Scene` two-light setup (main + fill at 30%) * shipped in v4.1.0 — AR scenes now match the 3D demo baseline (#1063). Pass `null` for * a single-light AR scene. ARCore's `ENVIRONMENTAL_HDR` estimate only drives * [mainLightNode]; [fillLightNode] keeps its baseline color/intensity each frame. */ fillLightNode: LightNode? = rememberFillLightNode(engine), cameraNode: ARCameraNode = rememberARCameraNode(engine), /** * Optional **absolute exposure scaling** applied to the AR camera. * * When non-null, this value is forwarded to Filament's * [io.github.sceneview.components.CameraComponent.setExposure] (single-`Float` * overload), which sets the aperture to 1.0, the shutter speed to 1.2 s, and * derives the sensitivity to match the requested **linear exposure value** * (1.0 ≈ ISO 100 ≈ EV 0). This is **not** a signed EV-stop bias — negative * values clamp the framebuffer to zero (full black, see #1179). Realistic * values land between ~0.05 (very dark) and ~16 (very bright). * * In practice, prefer leaving this `null`. The default exposure set by * [ARDefaultCameraNode] (f/12, 1/200 s, ISO 200 ≈ EV 11.6, after #1088) * matches the v4.1.0 main+fill light setup (10k + 3k lux) and is correct * for both back- and front-camera sessions on every device tested through * Pixel 9. Override only when porting a legacy AR scene that depends on * the linear-gain form. * * To darken / brighten by a number of stops instead, build a fresh * [ARCameraNode] and call the 3-arg `setExposure(aperture, shutter, ISO)` * on it (see [ARDefaultCameraNode] for the parity-correct values). */ cameraExposure: Float? = null, /** * Physics system to handle collision between nodes, hit testing on nodes, etc. */ collisionSystem: CollisionSystem = rememberCollisionSystem(view), /** * Used for [io.github.sceneview.node.ViewNode]s that can display an Android [android.view.View]. */ viewNodeWindowManager: WindowManager? = null, /** * The session is ready to be accessed. */ onSessionCreated: ((session: Session) -> Unit)? = null, /** * The session has been resumed. */ onSessionResumed: ((session: Session) -> Unit)? = null, /** * The session has been paused. */ onSessionPaused: ((session: Session) -> Unit)? = null, /** * Invoked when an ARCore error occurred during session creation / resume / configuration * (missing ARCore install, permission denied, device-unsupported, etc.). * * Playback-dataset failures (`PlaybackFailedException` or any exception thrown by * [Session.setPlaybackDataset]) are routed here ONLY when [onPlaybackFailed] is `null`. * Set [onPlaybackFailed] for fine-grained handling of "bad MP4 path" vs "AR unavailable". */ onSessionFailed: ((exception: Exception) -> Unit)? = null, /** * Optional dedicated callback for failures that originate from the * [playbackDataset] binding — typically `PlaybackFailedException` from ARCore when the * MP4 cannot be opened, parsed, or already has an active recording. * * When `null` (default), playback failures fall through to [onSessionFailed] for * backwards compatibility. */ onPlaybackFailed: ((exception: Exception) -> Unit)? = null, /** * Updates of the state of the ARCore system. * Invoked once per [Frame] immediately before the Scene is updated. */ onSessionUpdated: ((session: Session, frame: Frame) -> Unit)? = null, /** * Listen for camera tracking failure. */ onTrackingFailureChanged: ((trackingFailureReason: TrackingFailureReason?) -> Unit)? = null, /** * The listener invoked for all the gesture detector callbacks. */ onGestureListener: GestureDetector.OnGestureListener? = rememberOnGestureListener(), onTouchEvent: ((e: MotionEvent, hitResult: HitResult?) -> Boolean)? = null, permissionHandler: ARPermissionHandler? = (LocalContext.current as? androidx.activity.ComponentActivity)?.let { activity -> remember(activity) { ActivityARPermissionHandler(activity) } }, lifecycle: Lifecycle = LocalLifecycleOwner.current.lifecycle, /** * DSL block for declaring AR nodes via [ARSceneScope]. */ content: (@Composable ARSceneScope.() -> Unit)? = null )</ID>
    <ID>LongMethod:AugmentedFaceNode.kt:AugmentedFaceNode$override fun update(trackable: AugmentedFace?)</ID>
    <ID>LongMethod:LightEstimator.kt:LightEstimator$fun update(session: Session, frame: Frame, camera: Camera): Estimation?</ID>
    <ID>LongMethod:LightEstimatorConcurrentDestroyTest.kt:LightEstimatorConcurrentDestroyTest$@Test fun `100 iterations of concurrent update + destroy at 50 percent — zero crashes`()</ID>
    <ID>LoopWithTooManyJumpStatements:RerunBridge.kt:RerunBridge$while</ID>
    <ID>MatchingDeclarationName:ArSession.kt:ARSession : Session</ID>
    <ID>MaxLineLength:ARCompletenessDefaultsTest.kt:ARCompletenessDefaultsTest$"""DisposableEffect\(fillLightNode\)\s*\{\s*fillLightNode\?\.let\s*\{\s*nodeManager\.addNode\(it\)\s*\}\s*onDispose\s*\{\s*fillLightNode\?\.let\s*\{\s*nodeManager\.removeNode\(it\)\s*\}\s*\}\s*\}"""</ID>
    <ID>MaxLineLength:ARCompletenessDefaultsTest.kt:ARCompletenessDefaultsTest$"ARSceneView's fill-light wire-up MUST be a `DisposableEffect(fillLightNode) { add ; onDispose { remove } }` "</ID>
    <ID>MaxLineLength:ARCompletenessDefaultsTest.kt:ARCompletenessDefaultsTest$val</ID>
    <ID>MaxLineLength:ARCore.kt:ARCore$ReplaceWith("create(context, (context as? androidx.activity.ComponentActivity)?.let { ActivityARPermissionHandler(it) }, features)")</ID>
    <ID>MaxLineLength:ARCore.kt:ARCore$ReplaceWith("resume(context, (context as? androidx.activity.ComponentActivity)?.let { ActivityARPermissionHandler(it) })")</ID>
    <ID>MaxLineLength:ARScene.kt:@Deprecated("Use ARSceneView instead", ReplaceWith("ARSceneView(modifier, surfaceType, engine, modelLoader, materialLoader, environmentLoader, sessionFeatures, playbackDataset, sessionCameraConfig, sessionConfiguration, planeRenderer, cameraStream, view, isOpaque, renderer, scene, environment, mainLightNode, fillLightNode, cameraNode, cameraExposure, collisionSystem, viewNodeWindowManager, onSessionCreated, onSessionResumed, onSessionPaused, onSessionFailed, onPlaybackFailed, onSessionUpdated, onTrackingFailureChanged, onGestureListener, onTouchEvent, permissionHandler, lifecycle, content)"))</ID>
    <ID>MaxLineLength:ARScene.kt:permissionHandler</ID>
    <ID>MaxLineLength:RerunWireFormatEdgeCaseTest.kt:RerunWireFormatEdgeCaseTest$assertFalse("timestamp must not use 'E' notation", line.contains("E") &amp;&amp; line.indexOf("E") &lt; line.indexOf("\"type\""))</ID>
    <ID>NestedBlockDepth:LightEstimator.kt:LightEstimator$fun update(session: Session, frame: Frame, camera: Camera): Estimation?</ID>
    <ID>NestedBlockDepth:PlaneRenderer.kt:PlaneRenderer$fun update(session: Session, frame: Frame)</ID>
    <ID>NewLineAtEndOfFile:ARCameraNode.kt:io.github.sceneview.ar.node.ARCameraNode.kt</ID>
    <ID>NewLineAtEndOfFile:ARCameraStream.kt:io.github.sceneview.ar.camera.ARCameraStream.kt</ID>
    <ID>NewLineAtEndOfFile:Anchor.kt:io.github.sceneview.ar.scene.Anchor.kt</ID>
    <ID>NewLineAtEndOfFile:ArSession.kt:io.github.sceneview.ar.arcore.ArSession.kt</ID>
    <ID>NewLineAtEndOfFile:AugmentedImage.kt:io.github.sceneview.ar.arcore.AugmentedImage.kt</ID>
    <ID>NewLineAtEndOfFile:AugmentedImageNode.kt:io.github.sceneview.ar.node.AugmentedImageNode.kt</ID>
    <ID>NewLineAtEndOfFile:Camera.kt:io.github.sceneview.ar.arcore.Camera.kt</ID>
    <ID>NewLineAtEndOfFile:CloudAnchorNode.kt:io.github.sceneview.ar.node.CloudAnchorNode.kt</ID>
    <ID>NewLineAtEndOfFile:Frame.kt:io.github.sceneview.ar.arcore.Frame.kt</ID>
    <ID>NewLineAtEndOfFile:HitResultNode.kt:io.github.sceneview.ar.node.HitResultNode.kt</ID>
    <ID>NewLineAtEndOfFile:LightEstimator.kt:io.github.sceneview.ar.light.LightEstimator.kt</ID>
    <ID>NewLineAtEndOfFile:PlaneRenderer.kt:io.github.sceneview.ar.scene.PlaneRenderer.kt</ID>
    <ID>NewLineAtEndOfFile:Pose.kt:io.github.sceneview.ar.arcore.Pose.kt</ID>
    <ID>NewLineAtEndOfFile:RooftopAnchorNode.kt:io.github.sceneview.ar.node.RooftopAnchorNode.kt</ID>
    <ID>NewLineAtEndOfFile:StreetscapeGeometryNode.kt:io.github.sceneview.ar.node.StreetscapeGeometryNode.kt</ID>
    <ID>NewLineAtEndOfFile:TerrainAnchorNode.kt:io.github.sceneview.ar.node.TerrainAnchorNode.kt</ID>
    <ID>NewLineAtEndOfFile:Trackable.kt:io.github.sceneview.ar.arcore.Trackable.kt</ID>
    <ID>TooGenericExceptionThrown:RerunRateLimiterTest.kt:RerunRateLimiterTest$throw RuntimeException("server thread failed: ${e.message}", e)</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kCameraTextureParameter = "cameraTexture"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kDepthTextureParameter = "depthTexture"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kUVTransformParameter = "uvTransform"</ID>
    <ID>TopLevelPropertyNaming:ARCore.kt:/** * Assumed distance in meters from the device camera to the surface on which the user will * try to place models. * * This value affects the apparent scale of objects while the tracking method of the Instant * Placement point is `SCREENSPACE_WITH_APPROXIMATE_DISTANCE`. Values in the [0.2, 2.0] meter * range are a good choice for most AR experiences. */ const val kDefaultHitTestInstantDistance = 2.0f</ID>
    <ID>UnusedParameter:ARRecorderTest.kt:ARRecorderTest.ShadowCameraConfigFilter$direction: CameraConfig.FacingDirection?</ID>
    <ID>UnusedParameter:ARRecorderTest.kt:ARRecorderTest.ShadowCameraConfigFilter$targetFps: java.util.EnumSet&lt;CameraConfig.TargetFps>?</ID>
    <ID>UnusedParameter:ARScene.kt:/** * Used for [io.github.sceneview.node.ViewNode]s that can display an Android [android.view.View]. */ viewNodeWindowManager: WindowManager? = null</ID>
    <ID>UnusedParameter:ARScene.kt:view: View</ID>
    <ID>UnusedParameter:Frame.kt:trackable: T</ID>
    <ID>UnusedParameter:PlaneRenderer.kt:PlaneRenderer$session: Session</ID>
    <ID>UnusedParameter:RerunBridge.kt:RerunBridge$session: Session</ID>
  </CurrentIssues>
</SmellBaseline>
