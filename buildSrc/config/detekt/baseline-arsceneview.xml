<?xml version='1.0' encoding='UTF-8'?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>ComplexCondition:DepthMeshCollision.kt:i0 &lt; 0 || i1 &lt; 0 || i2 &lt; 0 || i0 + 2 >= posSize || i1 + 2 >= posSize || i2 + 2 >= posSize</ID>
    <ID>ComplexCondition:DepthMeshCollision.kt:triMaxX >= region.minX &amp;&amp; triMinX &lt;= region.maxX &amp;&amp; triMaxY >= region.minY &amp;&amp; triMinY &lt;= region.maxY &amp;&amp; triMaxZ >= region.minZ &amp;&amp; triMinZ &lt;= region.maxZ</ID>
    <ID>ComplexCondition:DepthMeshCollision.kt:x + radius &lt; minX || x - radius > maxX || z + radius &lt; minZ || z - radius > maxZ</ID>
    <ID>ComplexCondition:DepthMeshGeometry.kt:!valid[tl] || !valid[tr] || !valid[bl] || !valid[br]</ID>
    <ID>ConstructorParameterNaming:ARSceneScope.kt:ARSceneScope$_nodes: SnapshotStateList&lt;NodeImpl></ID>
    <ID>CyclomaticComplexMethod:ARScene.kt:@Composable fun ARSceneView( modifier: Modifier = Modifier, /** * Selects whether the backing surface is SurfaceView-based ([SurfaceType.Surface], renders * behind Compose, best performance) or TextureView-based ([SurfaceType.TextureSurface], * renders inline, supports alpha blending). */ surfaceType: SurfaceType = SurfaceType.Surface, /** * Provide your own instance if you want to share Filament resources between multiple views. */ engine: Engine = rememberEngine(), /** * Consumes a blob of glTF 2.0 content (either JSON or GLB) and produces a [Model] object, * which is a bundle of Filament textures, vertex buffers, index buffers, etc. */ modelLoader: ModelLoader = rememberModelLoader(engine), /** * A Filament Material defines the visual appearance of an object. * Materials function as templates from which [MaterialInstance]s can be spawned. */ materialLoader: MaterialLoader = rememberMaterialLoader(engine), /** * Utility for decoding an HDR file or consuming KTX1 files and producing Filament textures, * IBLs, and sky boxes. */ environmentLoader: EnvironmentLoader = rememberEnvironmentLoader(engine), /** * Fundamental session features that can be requested. * @see Session.Feature */ sessionFeatures: Set&lt;Session.Feature> = setOf(), /** * Optional MP4 dataset [File] to play back instead of the live camera feed. * * When non-null, ARCore is configured for **playback** mode: the session re-runs the * recorded camera frames, IMU data, planes, anchors and depth from the dataset, exactly * as captured by a previous call to * [Session.startRecording][com.google.ar.core.Session.startRecording] (or via the * [io.github.sceneview.ar.recording.ARRecorder] helper). This is the standard ARCore * record-replay workflow — capture an outdoor session once, iterate at the desk against * the recording, share the MP4 with teammates to reproduce bugs deterministically. * * The file must be passed before the session resumes; SceneView wires it on session * creation. Switching between live and playback at runtime requires the [ARSceneView] * to be fully recreated (e.g. via Compose `key(playbackDataset) { … }`), because ARCore * binds the playback source to the [Session] for its entire lifetime. * * Default `null` (live camera mode). */ playbackDataset: File? = null, /** * Optional scoped-storage [android.net.Uri] dataset to play back instead of the live camera * feed (Android 10+ / API 29+ alternative to [playbackDataset]). * * Wraps ARCore's [com.google.ar.core.Session.setPlaybackDatasetUri] (#1770), which accepts * `content://` URIs (`MediaStore`, Storage Access Framework picker output, app-private * file providers). The legacy `playbackDataset: File?` only accepts an absolute filesystem * path — which on Android 10+ scoped storage means the app must copy the user-picked MP4 * into its sandbox before replay. With `playbackDatasetUri` the replay reads the original * `Uri` directly. * * **Scheme allowlist (#1845).** Only `content://` and `file://` URIs are accepted — * passing e.g. `https://`, `data:`, or any custom scheme triggers an * `IllegalArgumentException` at session creation. ARCore would silently fail or hand the * URI off to an arbitrary `ContentResolver` registration; we surface the misuse at the * SceneView boundary where the caller can see it. * * **Caller-side permission requirement.** A `content://` URI is only readable by ARCore * if the calling process has been granted read access to it. With a Storage Access Framework * picker (`ACTION_OPEN_DOCUMENT`), the grant is implicit for the lifetime of the activity; * to outlive process restarts call * [ContentResolver.takePersistableUriPermission][android.content.ContentResolver.takePersistableUriPermission]. * When forwarding the URI to another component via [android.content.Intent], set * [android.content.Intent.FLAG_GRANT_READ_URI_PERMISSION] on the launcher intent. Without * a read grant ARCore raises [PlaybackFailedException] which is routed to * [onPlaybackFailed]. * * **Mutually exclusive with [playbackDataset].** Setting both is a programming error and * triggers an `IllegalArgumentException` at session creation. Default `null`. * * Same snapshot semantics as [playbackDataset] — the value is captured at first composition * (ARCore requires the playback source to be set before the first resume). Switching * between live and playback at runtime requires wrapping the `ARSceneView` in * `key(playbackDatasetUri) { … }` so Compose rebuilds the session. */ playbackDatasetUri: android.net.Uri? = null, /** * Selects the camera config to use. The returned config must be one returned by * [Session.getSupportedCameraConfigs]. * * Defaults to [highestResolutionCameraConfig] so every AR scene — and in particular every * [ARRecorder] recording — runs at the device's full back-camera resolution rather than * ARCore's low-res 640×480 CPU-stream default ([#1065](https://github.com/sceneview/sceneview/issues/1065)). * Pass `null` to keep ARCore's stock default config, [::frontCameraConfig] for an Augmented * Faces session, or supply a custom selector (e.g. `cameraConfigFilter { … }`). * * **⚠️ Swapping the camera config mid-session is disruptive.** This selector is reactive — * changing it (e.g. BACK→FRONT for Augmented Faces) is applied to the live [Session], but * ARCore restarts the camera pipeline: a frame drop / black flash, total tracking loss * (anchors invalidated, planes + world frame re-acquired, plane discontinuities), and a * depth-buffer format change (front-camera configs expose no ARCore Depth, so occlusion * silently downgrades). For a clean swap, wrap the `ARSceneView` in `key(facing) { … }` and * show a spinner over the flash — see the "Camera config swap" example in `llms.txt`. */ sessionCameraConfig: ((Session) -> CameraConfig)? = ::highestResolutionCameraConfig, /** * Drives the device torch during the AR session — ARCore v1.45+ Flash Mode API (#1732). * * - [Config.FlashMode.OFF] (default): no torch. * - [Config.FlashMode.TORCH]: ARCore keeps the back-camera LED on while the session is * running. Helps tracking in low-light scenes; battery cost is significant — toggle off * once the user re-enters daylight. * * **Support-gated.** Flash Mode requires (a) ARCore 1.45+ runtime on-device, and (b) a * back-camera config — front-camera sessions never expose a torch. If the device or the * current camera config does not support the requested mode, SceneView silently downgrades * the session to `OFF` (matching the auto-fallback used for unsupported `depthMode`). Apps * that want to surface the support state to the user can call * [com.google.ar.core.Session.isSupported] with a config carrying the desired * [Config.FlashMode] directly. */ flashMode: Config.FlashMode = Config.FlashMode.OFF, /** * ARCore [Config.PlaneFindingMode] — controls which plane orientations the session tracks. * Defaults to [Config.PlaneFindingMode.HORIZONTAL_AND_VERTICAL] (ARCore's recommended value * for surface-tap demos). Applied BEFORE [sessionConfiguration], so the callback still wins * (#1766). */ planeFindingMode: Config.PlaneFindingMode = Config.PlaneFindingMode.HORIZONTAL_AND_VERTICAL, /** * ARCore [Config.DepthMode] — enables motion-stereo depth so [Frame.acquireDepthImage16Bits], * `Frame.hitTestDepth`, [io.github.sceneview.ar.node.DepthMeshNode] and ARCameraStream * occlusion all see real-world depth. Defaults to [Config.DepthMode.DISABLED] (ARCore's * stock default — the depth pipeline is opt-in for power reasons). Support-gated — if * the device does not support the requested mode, [ARSession.configure] silently downgrades * to `DISABLED`. Pair with [Config.GeospatialMode.ENABLED] + * [Config.StreetscapeGeometryMode.ENABLED] to unlock ARCore 1.54's Geospatial Depth (~65 m, * see #1731). Applied BEFORE [sessionConfiguration] (#1766). */ depthMode: Config.DepthMode = Config.DepthMode.DISABLED, /** * ARCore [Config.InstantPlacementMode] — places anchors before a plane has been found by * estimating the surface from screen-space cues. Defaults to * [Config.InstantPlacementMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ instantPlacementMode: Config.InstantPlacementMode = Config.InstantPlacementMode.DISABLED, /** * ARCore [Config.GeospatialMode] — unlocks Earth-anchored tracking (Terrain/Rooftop * anchors, [GeospatialPose], the VPS-backed coordinate frame). Requires the ARCore Cloud * API key and `ACCESS_FINE_LOCATION` permission — without those, ARCore throws * `FineLocationPermissionNotGrantedException` on resume. Defaults to * [Config.GeospatialMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ geospatialMode: Config.GeospatialMode = Config.GeospatialMode.DISABLED, /** * ARCore [Config.StreetscapeGeometryMode] — exposes Geospatial Streetscape Geometry * (buildings + terrain meshes around the user). Requires * `geospatialMode = Config.GeospatialMode.ENABLED`; otherwise ARCore rejects the config. * Defaults to [Config.StreetscapeGeometryMode.DISABLED]. Applied BEFORE * [sessionConfiguration] (#1766). */ streetscapeGeometryMode: Config.StreetscapeGeometryMode = Config.StreetscapeGeometryMode.DISABLED, /** * ARCore [Config.CloudAnchorMode] — enables host/resolve of [CloudAnchorNode]s. Requires * the ARCore Cloud API key. Defaults to [Config.CloudAnchorMode.DISABLED]. Applied BEFORE * [sessionConfiguration] (#1766). */ cloudAnchorMode: Config.CloudAnchorMode = Config.CloudAnchorMode.DISABLED, /** * ARCore [Config.AugmentedFaceMode] — enables face mesh tracking through * [AugmentedFaceNode][io.github.sceneview.ar.node.AugmentedFaceNode]. Requires a * front-facing camera session feature (`Session.Feature.FRONT_CAMERA`). Defaults to * [Config.AugmentedFaceMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ augmentedFaceMode: Config.AugmentedFaceMode = Config.AugmentedFaceMode.DISABLED, /** * ARCore [Config.ImageStabilizationMode] — enables EIS-aligned camera-stream rendering. * Defaults to [Config.ImageStabilizationMode.OFF]. Applied BEFORE [sessionConfiguration] * (#1766). */ imageStabilizationMode: Config.ImageStabilizationMode = Config.ImageStabilizationMode.OFF, /** * ARCore [Config.SemanticMode] — exposes Scene Semantics segmentation labels via * `Frame.acquireSemanticImage` / `acquireSemanticConfidenceImage`. Defaults to * [Config.SemanticMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ semanticMode: Config.SemanticMode = Config.SemanticMode.DISABLED, /** * ARCore [Config.UpdateMode] — controls whether [Session.update] blocks until a new camera * frame is available (`LATEST_CAMERA_IMAGE`) or returns immediately (`BLOCKING`). Defaults * to [Config.UpdateMode.LATEST_CAMERA_IMAGE], the value SceneView's render loop is built * for. Applied BEFORE [sessionConfiguration] (#1766). */ updateMode: Config.UpdateMode = Config.UpdateMode.LATEST_CAMERA_IMAGE, /** * ARCore [Config.FocusMode] — `FIXED` to disable auto-focus (best for sharp far-field * tracking — what ARCore recommends for most AR experiences), `AUTO` to let the camera * driver autofocus. SceneView defaults to [Config.FocusMode.AUTO] for parity with previous * behaviour. Applied BEFORE [sessionConfiguration] (#1766). */ focusMode: Config.FocusMode = Config.FocusMode.AUTO, /** * Configures the session and verifies that the enabled features in the specified session * config are supported with the currently set camera config. * * **Precedence (#1766):** all typed `*Mode` params above are applied to the [Config] * BEFORE this callback runs, so the callback still wins. Use the typed params for the * common cases (AI codegen, demo boilerplate) and this callback as the escape hatch for * any [Config] property without a dedicated param. * * **⚠️ Mid-session config changes are accepted but not free.** This callback is reactive — * editing it re-runs [Session.configure] on the live session. Most `Config` knobs * reconfigure cheaply, but some force a camera-pipeline restart with a visible frame drop * and brief tracking loss — notably toggling [Config.DepthMode] or any change that switches * the camera config (see [sessionCameraConfig]). When in doubt, wrap the `ARSceneView` in * `key(...) { … }` keyed on the changing value so Compose rebuilds the session cleanly. */ sessionConfiguration: ((session: Session, Config) -> Unit)? = null, /** * Enable the plane renderer. */ planeRenderer: Boolean = true, /** * Grouped scene-understanding flags (#1767) — mirrors RealityKit's * `ARView.environment.sceneUnderstanding.options`. When non-null, the four * inner flags (`occlusion`, `lighting`, `physics`, `planeVisualization`) * are applied in tandem, overriding the individual scattered flags * (`planeRenderer`, [ARCameraStream.isDepthOcclusionEnabled], * [io.github.sceneview.ar.light.LightEstimator.isEnabled]). * * Default `null` keeps every individual flag at its pre-#1767 default — * the grouped knob is purely additive and opt-in. Use it when you want a * single discoverable point of configuration; keep it null and mutate the * individual flags directly if you need fine-grained control. */ sceneUnderstanding: SceneUnderstanding? = null, /** * The [ARCameraStream] to render the camera texture. */ cameraStream: ARCameraStream? = rememberARCameraStream(materialLoader), /** * Encompasses all the state needed for rendering a [SceneView]. */ view: View = rememberARView(engine), /** * Controls whether the render target is opaque or not. Default `true`. */ isOpaque: Boolean = true, /** * A [Renderer] instance represents an operating system's window. */ renderer: Renderer = rememberRenderer(engine), /** * Provide your own instance if you want to share [Node]s' scene between multiple views. */ scene: Scene = rememberScene(engine), /** * Defines the lighting environment and the skybox of the scene. * * Defaults to [rememberAREnvironment] with an [EnvironmentLoader], which ships * the neutral IBL baseline so PBR materials reflect something sensible until * ARCore's `ENVIRONMENTAL_HDR` light estimate stabilises (#1063). */ environment: Environment = rememberAREnvironment(engine), /** * Always add a direct light source since it is required for shadowing. */ mainLightNode: LightNode? = rememberMainLightNode(engine), /** * Optional secondary "fill" directional light that softens the shadows produced by * [mainLightNode]. Default mirrors the 3D `Scene` two-light setup (main + fill at 30%) * shipped in v4.1.0 — AR scenes now match the 3D demo baseline (#1063). Pass `null` for * a single-light AR scene. ARCore's `ENVIRONMENTAL_HDR` estimate only drives * [mainLightNode]; [fillLightNode] keeps its baseline color/intensity each frame. */ fillLightNode: LightNode? = rememberFillLightNode(engine), cameraNode: ARCameraNode = rememberARCameraNode(engine), /** * Optional **absolute exposure scaling** applied to the AR camera. * * When non-null, this value is forwarded to Filament's * [io.github.sceneview.components.CameraComponent.setExposure] (single-`Float` * overload), which sets the aperture to 1.0, the shutter speed to 1.2 s, and * derives the sensitivity to match the requested **linear exposure value** * (1.0 ≈ ISO 100 ≈ EV 0). This is **not** a signed EV-stop bias — negative * values clamp the framebuffer to zero (full black, see #1179). Realistic * values land between ~0.05 (very dark) and ~16 (very bright). * * In practice, prefer leaving this `null`. The default exposure set by * [ARDefaultCameraNode] (f/12, 1/200 s, ISO 200 ≈ EV 11.6, after #1088) * matches the v4.1.0 main+fill light setup (10k + 3k lux) and is correct * for both back- and front-camera sessions on every device tested through * Pixel 9. Override only when porting a legacy AR scene that depends on * the linear-gain form. * * To darken / brighten by a number of stops instead, build a fresh * [ARCameraNode] and call the 3-arg `setExposure(aperture, shutter, ISO)` * on it (see [ARDefaultCameraNode] for the parity-correct values). */ cameraExposure: Float? = null, /** * Physics system to handle collision between nodes, hit testing on nodes, etc. */ collisionSystem: CollisionSystem = rememberCollisionSystem(view), /** * Used for [io.github.sceneview.node.ViewNode]s that can display an Android [android.view.View]. */ viewNodeWindowManager: WindowManager? = null, /** * The session is ready to be accessed. */ onSessionCreated: ((session: Session) -> Unit)? = null, /** * The session has been resumed. */ onSessionResumed: ((session: Session) -> Unit)? = null, /** * The session has been paused. */ onSessionPaused: ((session: Session) -> Unit)? = null, /** * Invoked when an ARCore error occurred during session creation / resume / configuration * (missing ARCore install, permission denied, device-unsupported, etc.). * * Playback-dataset failures (`PlaybackFailedException` or any exception thrown by * [Session.setPlaybackDataset]) are routed here ONLY when [onPlaybackFailed] is `null`. * Set [onPlaybackFailed] for fine-grained handling of "bad MP4 path" vs "AR unavailable". * * **Soft-deprecated (#1844):** prefer [onSessionFailure] which delivers a sealed * [ARSessionFailure] subtype so the compiler enforces exhaustive `when` matching * (#1759). The raw callback is kept un-deprecated at the Kotlin level for * backwards compatibility — both fire when set — but new code should wire only * [onSessionFailure]. */ onSessionFailed: ((exception: Exception) -> Unit)? = null, /** * Typed equivalent of [onSessionFailed] (#1759). Receives an [ARSessionFailure] sealed-class * instance instead of a raw [Exception], so the callback can do an exhaustive `when` over * every ARCore failure mode (install, permission, camera, cloud-anchor, …) — the compiler * catches the day a new failure category is added and the app forgot to handle it. * * **Both callbacks fire** when set — [onSessionFailed] runs first (raw `Exception`), * [onSessionFailure] runs second (mapped). Apps migrating off the raw form can wire the * typed callback alongside and gradually delete the legacy one; setting only * [onSessionFailure] is the new recommended path. */ onSessionFailure: ((failure: ARSessionFailure) -> Unit)? = null, /** * Optional dedicated callback for failures that originate from the * [playbackDataset] binding — typically `PlaybackFailedException` from ARCore when the * MP4 cannot be opened, parsed, or already has an active recording. * * When `null` (default), playback failures fall through to [onSessionFailed] for * backwards compatibility. */ onPlaybackFailed: ((exception: Exception) -> Unit)? = null, /** * Updates of the state of the ARCore system. * Invoked once per [Frame] immediately before the Scene is updated. */ onSessionUpdated: ((session: Session, frame: Frame) -> Unit)? = null, /** * Listen for camera tracking failure. */ onTrackingFailureChanged: ((trackingFailureReason: TrackingFailureReason?) -> Unit)? = null, /** * The listener invoked for all the gesture detector callbacks. */ onGestureListener: GestureDetector.OnGestureListener? = rememberOnGestureListener(), onTouchEvent: ((e: MotionEvent, hitResult: HitResult?) -> Boolean)? = null, permissionHandler: ARPermissionHandler? = (LocalContext.current as? androidx.activity.ComponentActivity)?.let { activity -> remember(activity) { ActivityARPermissionHandler(activity) } }, lifecycle: Lifecycle = LocalLifecycleOwner.current.lifecycle, /** * DSL block for declaring AR nodes via [ARSceneScope]. */ content: (@Composable ARSceneScope.() -> Unit)? = null )</ID>
    <ID>CyclomaticComplexMethod:ARSessionFailure.kt:ARSessionFailure.Companion$@JvmStatic public fun from(exception: Exception): ARSessionFailure</ID>
    <ID>CyclomaticComplexMethod:DepthMeshCollision.kt:@Suppress("UNUSED_PARAMETER") // y reserved for a future 3D variant — keep stable API internal fun nearestSurfaceYBelow( positionsWorld: FloatArray, indices: IntArray, x: Float, y: Float, z: Float, radius: Float, ): Float?</ID>
    <ID>CyclomaticComplexMethod:LightEstimator.kt:LightEstimator$fun update(session: Session, frame: Frame, camera: Camera): Estimation?</ID>
    <ID>DestructuringDeclarationWithTooManyEntries:ARFogNodeTest.kt:ARFogNodeTest$(depth, density, start, end)</ID>
    <ID>LongMethod:ARScene.kt:@Composable fun ARSceneView( modifier: Modifier = Modifier, /** * Selects whether the backing surface is SurfaceView-based ([SurfaceType.Surface], renders * behind Compose, best performance) or TextureView-based ([SurfaceType.TextureSurface], * renders inline, supports alpha blending). */ surfaceType: SurfaceType = SurfaceType.Surface, /** * Provide your own instance if you want to share Filament resources between multiple views. */ engine: Engine = rememberEngine(), /** * Consumes a blob of glTF 2.0 content (either JSON or GLB) and produces a [Model] object, * which is a bundle of Filament textures, vertex buffers, index buffers, etc. */ modelLoader: ModelLoader = rememberModelLoader(engine), /** * A Filament Material defines the visual appearance of an object. * Materials function as templates from which [MaterialInstance]s can be spawned. */ materialLoader: MaterialLoader = rememberMaterialLoader(engine), /** * Utility for decoding an HDR file or consuming KTX1 files and producing Filament textures, * IBLs, and sky boxes. */ environmentLoader: EnvironmentLoader = rememberEnvironmentLoader(engine), /** * Fundamental session features that can be requested. * @see Session.Feature */ sessionFeatures: Set&lt;Session.Feature> = setOf(), /** * Optional MP4 dataset [File] to play back instead of the live camera feed. * * When non-null, ARCore is configured for **playback** mode: the session re-runs the * recorded camera frames, IMU data, planes, anchors and depth from the dataset, exactly * as captured by a previous call to * [Session.startRecording][com.google.ar.core.Session.startRecording] (or via the * [io.github.sceneview.ar.recording.ARRecorder] helper). This is the standard ARCore * record-replay workflow — capture an outdoor session once, iterate at the desk against * the recording, share the MP4 with teammates to reproduce bugs deterministically. * * The file must be passed before the session resumes; SceneView wires it on session * creation. Switching between live and playback at runtime requires the [ARSceneView] * to be fully recreated (e.g. via Compose `key(playbackDataset) { … }`), because ARCore * binds the playback source to the [Session] for its entire lifetime. * * Default `null` (live camera mode). */ playbackDataset: File? = null, /** * Optional scoped-storage [android.net.Uri] dataset to play back instead of the live camera * feed (Android 10+ / API 29+ alternative to [playbackDataset]). * * Wraps ARCore's [com.google.ar.core.Session.setPlaybackDatasetUri] (#1770), which accepts * `content://` URIs (`MediaStore`, Storage Access Framework picker output, app-private * file providers). The legacy `playbackDataset: File?` only accepts an absolute filesystem * path — which on Android 10+ scoped storage means the app must copy the user-picked MP4 * into its sandbox before replay. With `playbackDatasetUri` the replay reads the original * `Uri` directly. * * **Scheme allowlist (#1845).** Only `content://` and `file://` URIs are accepted — * passing e.g. `https://`, `data:`, or any custom scheme triggers an * `IllegalArgumentException` at session creation. ARCore would silently fail or hand the * URI off to an arbitrary `ContentResolver` registration; we surface the misuse at the * SceneView boundary where the caller can see it. * * **Caller-side permission requirement.** A `content://` URI is only readable by ARCore * if the calling process has been granted read access to it. With a Storage Access Framework * picker (`ACTION_OPEN_DOCUMENT`), the grant is implicit for the lifetime of the activity; * to outlive process restarts call * [ContentResolver.takePersistableUriPermission][android.content.ContentResolver.takePersistableUriPermission]. * When forwarding the URI to another component via [android.content.Intent], set * [android.content.Intent.FLAG_GRANT_READ_URI_PERMISSION] on the launcher intent. Without * a read grant ARCore raises [PlaybackFailedException] which is routed to * [onPlaybackFailed]. * * **Mutually exclusive with [playbackDataset].** Setting both is a programming error and * triggers an `IllegalArgumentException` at session creation. Default `null`. * * Same snapshot semantics as [playbackDataset] — the value is captured at first composition * (ARCore requires the playback source to be set before the first resume). Switching * between live and playback at runtime requires wrapping the `ARSceneView` in * `key(playbackDatasetUri) { … }` so Compose rebuilds the session. */ playbackDatasetUri: android.net.Uri? = null, /** * Selects the camera config to use. The returned config must be one returned by * [Session.getSupportedCameraConfigs]. * * Defaults to [highestResolutionCameraConfig] so every AR scene — and in particular every * [ARRecorder] recording — runs at the device's full back-camera resolution rather than * ARCore's low-res 640×480 CPU-stream default ([#1065](https://github.com/sceneview/sceneview/issues/1065)). * Pass `null` to keep ARCore's stock default config, [::frontCameraConfig] for an Augmented * Faces session, or supply a custom selector (e.g. `cameraConfigFilter { … }`). * * **⚠️ Swapping the camera config mid-session is disruptive.** This selector is reactive — * changing it (e.g. BACK→FRONT for Augmented Faces) is applied to the live [Session], but * ARCore restarts the camera pipeline: a frame drop / black flash, total tracking loss * (anchors invalidated, planes + world frame re-acquired, plane discontinuities), and a * depth-buffer format change (front-camera configs expose no ARCore Depth, so occlusion * silently downgrades). For a clean swap, wrap the `ARSceneView` in `key(facing) { … }` and * show a spinner over the flash — see the "Camera config swap" example in `llms.txt`. */ sessionCameraConfig: ((Session) -> CameraConfig)? = ::highestResolutionCameraConfig, /** * Drives the device torch during the AR session — ARCore v1.45+ Flash Mode API (#1732). * * - [Config.FlashMode.OFF] (default): no torch. * - [Config.FlashMode.TORCH]: ARCore keeps the back-camera LED on while the session is * running. Helps tracking in low-light scenes; battery cost is significant — toggle off * once the user re-enters daylight. * * **Support-gated.** Flash Mode requires (a) ARCore 1.45+ runtime on-device, and (b) a * back-camera config — front-camera sessions never expose a torch. If the device or the * current camera config does not support the requested mode, SceneView silently downgrades * the session to `OFF` (matching the auto-fallback used for unsupported `depthMode`). Apps * that want to surface the support state to the user can call * [com.google.ar.core.Session.isSupported] with a config carrying the desired * [Config.FlashMode] directly. */ flashMode: Config.FlashMode = Config.FlashMode.OFF, /** * ARCore [Config.PlaneFindingMode] — controls which plane orientations the session tracks. * Defaults to [Config.PlaneFindingMode.HORIZONTAL_AND_VERTICAL] (ARCore's recommended value * for surface-tap demos). Applied BEFORE [sessionConfiguration], so the callback still wins * (#1766). */ planeFindingMode: Config.PlaneFindingMode = Config.PlaneFindingMode.HORIZONTAL_AND_VERTICAL, /** * ARCore [Config.DepthMode] — enables motion-stereo depth so [Frame.acquireDepthImage16Bits], * `Frame.hitTestDepth`, [io.github.sceneview.ar.node.DepthMeshNode] and ARCameraStream * occlusion all see real-world depth. Defaults to [Config.DepthMode.DISABLED] (ARCore's * stock default — the depth pipeline is opt-in for power reasons). Support-gated — if * the device does not support the requested mode, [ARSession.configure] silently downgrades * to `DISABLED`. Pair with [Config.GeospatialMode.ENABLED] + * [Config.StreetscapeGeometryMode.ENABLED] to unlock ARCore 1.54's Geospatial Depth (~65 m, * see #1731). Applied BEFORE [sessionConfiguration] (#1766). */ depthMode: Config.DepthMode = Config.DepthMode.DISABLED, /** * ARCore [Config.InstantPlacementMode] — places anchors before a plane has been found by * estimating the surface from screen-space cues. Defaults to * [Config.InstantPlacementMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ instantPlacementMode: Config.InstantPlacementMode = Config.InstantPlacementMode.DISABLED, /** * ARCore [Config.GeospatialMode] — unlocks Earth-anchored tracking (Terrain/Rooftop * anchors, [GeospatialPose], the VPS-backed coordinate frame). Requires the ARCore Cloud * API key and `ACCESS_FINE_LOCATION` permission — without those, ARCore throws * `FineLocationPermissionNotGrantedException` on resume. Defaults to * [Config.GeospatialMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ geospatialMode: Config.GeospatialMode = Config.GeospatialMode.DISABLED, /** * ARCore [Config.StreetscapeGeometryMode] — exposes Geospatial Streetscape Geometry * (buildings + terrain meshes around the user). Requires * `geospatialMode = Config.GeospatialMode.ENABLED`; otherwise ARCore rejects the config. * Defaults to [Config.StreetscapeGeometryMode.DISABLED]. Applied BEFORE * [sessionConfiguration] (#1766). */ streetscapeGeometryMode: Config.StreetscapeGeometryMode = Config.StreetscapeGeometryMode.DISABLED, /** * ARCore [Config.CloudAnchorMode] — enables host/resolve of [CloudAnchorNode]s. Requires * the ARCore Cloud API key. Defaults to [Config.CloudAnchorMode.DISABLED]. Applied BEFORE * [sessionConfiguration] (#1766). */ cloudAnchorMode: Config.CloudAnchorMode = Config.CloudAnchorMode.DISABLED, /** * ARCore [Config.AugmentedFaceMode] — enables face mesh tracking through * [AugmentedFaceNode][io.github.sceneview.ar.node.AugmentedFaceNode]. Requires a * front-facing camera session feature (`Session.Feature.FRONT_CAMERA`). Defaults to * [Config.AugmentedFaceMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ augmentedFaceMode: Config.AugmentedFaceMode = Config.AugmentedFaceMode.DISABLED, /** * ARCore [Config.ImageStabilizationMode] — enables EIS-aligned camera-stream rendering. * Defaults to [Config.ImageStabilizationMode.OFF]. Applied BEFORE [sessionConfiguration] * (#1766). */ imageStabilizationMode: Config.ImageStabilizationMode = Config.ImageStabilizationMode.OFF, /** * ARCore [Config.SemanticMode] — exposes Scene Semantics segmentation labels via * `Frame.acquireSemanticImage` / `acquireSemanticConfidenceImage`. Defaults to * [Config.SemanticMode.DISABLED]. Applied BEFORE [sessionConfiguration] (#1766). */ semanticMode: Config.SemanticMode = Config.SemanticMode.DISABLED, /** * ARCore [Config.UpdateMode] — controls whether [Session.update] blocks until a new camera * frame is available (`LATEST_CAMERA_IMAGE`) or returns immediately (`BLOCKING`). Defaults * to [Config.UpdateMode.LATEST_CAMERA_IMAGE], the value SceneView's render loop is built * for. Applied BEFORE [sessionConfiguration] (#1766). */ updateMode: Config.UpdateMode = Config.UpdateMode.LATEST_CAMERA_IMAGE, /** * ARCore [Config.FocusMode] — `FIXED` to disable auto-focus (best for sharp far-field * tracking — what ARCore recommends for most AR experiences), `AUTO` to let the camera * driver autofocus. SceneView defaults to [Config.FocusMode.AUTO] for parity with previous * behaviour. Applied BEFORE [sessionConfiguration] (#1766). */ focusMode: Config.FocusMode = Config.FocusMode.AUTO, /** * Configures the session and verifies that the enabled features in the specified session * config are supported with the currently set camera config. * * **Precedence (#1766):** all typed `*Mode` params above are applied to the [Config] * BEFORE this callback runs, so the callback still wins. Use the typed params for the * common cases (AI codegen, demo boilerplate) and this callback as the escape hatch for * any [Config] property without a dedicated param. * * **⚠️ Mid-session config changes are accepted but not free.** This callback is reactive — * editing it re-runs [Session.configure] on the live session. Most `Config` knobs * reconfigure cheaply, but some force a camera-pipeline restart with a visible frame drop * and brief tracking loss — notably toggling [Config.DepthMode] or any change that switches * the camera config (see [sessionCameraConfig]). When in doubt, wrap the `ARSceneView` in * `key(...) { … }` keyed on the changing value so Compose rebuilds the session cleanly. */ sessionConfiguration: ((session: Session, Config) -> Unit)? = null, /** * Enable the plane renderer. */ planeRenderer: Boolean = true, /** * Grouped scene-understanding flags (#1767) — mirrors RealityKit's * `ARView.environment.sceneUnderstanding.options`. When non-null, the four * inner flags (`occlusion`, `lighting`, `physics`, `planeVisualization`) * are applied in tandem, overriding the individual scattered flags * (`planeRenderer`, [ARCameraStream.isDepthOcclusionEnabled], * [io.github.sceneview.ar.light.LightEstimator.isEnabled]). * * Default `null` keeps every individual flag at its pre-#1767 default — * the grouped knob is purely additive and opt-in. Use it when you want a * single discoverable point of configuration; keep it null and mutate the * individual flags directly if you need fine-grained control. */ sceneUnderstanding: SceneUnderstanding? = null, /** * The [ARCameraStream] to render the camera texture. */ cameraStream: ARCameraStream? = rememberARCameraStream(materialLoader), /** * Encompasses all the state needed for rendering a [SceneView]. */ view: View = rememberARView(engine), /** * Controls whether the render target is opaque or not. Default `true`. */ isOpaque: Boolean = true, /** * A [Renderer] instance represents an operating system's window. */ renderer: Renderer = rememberRenderer(engine), /** * Provide your own instance if you want to share [Node]s' scene between multiple views. */ scene: Scene = rememberScene(engine), /** * Defines the lighting environment and the skybox of the scene. * * Defaults to [rememberAREnvironment] with an [EnvironmentLoader], which ships * the neutral IBL baseline so PBR materials reflect something sensible until * ARCore's `ENVIRONMENTAL_HDR` light estimate stabilises (#1063). */ environment: Environment = rememberAREnvironment(engine), /** * Always add a direct light source since it is required for shadowing. */ mainLightNode: LightNode? = rememberMainLightNode(engine), /** * Optional secondary "fill" directional light that softens the shadows produced by * [mainLightNode]. Default mirrors the 3D `Scene` two-light setup (main + fill at 30%) * shipped in v4.1.0 — AR scenes now match the 3D demo baseline (#1063). Pass `null` for * a single-light AR scene. ARCore's `ENVIRONMENTAL_HDR` estimate only drives * [mainLightNode]; [fillLightNode] keeps its baseline color/intensity each frame. */ fillLightNode: LightNode? = rememberFillLightNode(engine), cameraNode: ARCameraNode = rememberARCameraNode(engine), /** * Optional **absolute exposure scaling** applied to the AR camera. * * When non-null, this value is forwarded to Filament's * [io.github.sceneview.components.CameraComponent.setExposure] (single-`Float` * overload), which sets the aperture to 1.0, the shutter speed to 1.2 s, and * derives the sensitivity to match the requested **linear exposure value** * (1.0 ≈ ISO 100 ≈ EV 0). This is **not** a signed EV-stop bias — negative * values clamp the framebuffer to zero (full black, see #1179). Realistic * values land between ~0.05 (very dark) and ~16 (very bright). * * In practice, prefer leaving this `null`. The default exposure set by * [ARDefaultCameraNode] (f/12, 1/200 s, ISO 200 ≈ EV 11.6, after #1088) * matches the v4.1.0 main+fill light setup (10k + 3k lux) and is correct * for both back- and front-camera sessions on every device tested through * Pixel 9. Override only when porting a legacy AR scene that depends on * the linear-gain form. * * To darken / brighten by a number of stops instead, build a fresh * [ARCameraNode] and call the 3-arg `setExposure(aperture, shutter, ISO)` * on it (see [ARDefaultCameraNode] for the parity-correct values). */ cameraExposure: Float? = null, /** * Physics system to handle collision between nodes, hit testing on nodes, etc. */ collisionSystem: CollisionSystem = rememberCollisionSystem(view), /** * Used for [io.github.sceneview.node.ViewNode]s that can display an Android [android.view.View]. */ viewNodeWindowManager: WindowManager? = null, /** * The session is ready to be accessed. */ onSessionCreated: ((session: Session) -> Unit)? = null, /** * The session has been resumed. */ onSessionResumed: ((session: Session) -> Unit)? = null, /** * The session has been paused. */ onSessionPaused: ((session: Session) -> Unit)? = null, /** * Invoked when an ARCore error occurred during session creation / resume / configuration * (missing ARCore install, permission denied, device-unsupported, etc.). * * Playback-dataset failures (`PlaybackFailedException` or any exception thrown by * [Session.setPlaybackDataset]) are routed here ONLY when [onPlaybackFailed] is `null`. * Set [onPlaybackFailed] for fine-grained handling of "bad MP4 path" vs "AR unavailable". * * **Soft-deprecated (#1844):** prefer [onSessionFailure] which delivers a sealed * [ARSessionFailure] subtype so the compiler enforces exhaustive `when` matching * (#1759). The raw callback is kept un-deprecated at the Kotlin level for * backwards compatibility — both fire when set — but new code should wire only * [onSessionFailure]. */ onSessionFailed: ((exception: Exception) -> Unit)? = null, /** * Typed equivalent of [onSessionFailed] (#1759). Receives an [ARSessionFailure] sealed-class * instance instead of a raw [Exception], so the callback can do an exhaustive `when` over * every ARCore failure mode (install, permission, camera, cloud-anchor, …) — the compiler * catches the day a new failure category is added and the app forgot to handle it. * * **Both callbacks fire** when set — [onSessionFailed] runs first (raw `Exception`), * [onSessionFailure] runs second (mapped). Apps migrating off the raw form can wire the * typed callback alongside and gradually delete the legacy one; setting only * [onSessionFailure] is the new recommended path. */ onSessionFailure: ((failure: ARSessionFailure) -> Unit)? = null, /** * Optional dedicated callback for failures that originate from the * [playbackDataset] binding — typically `PlaybackFailedException` from ARCore when the * MP4 cannot be opened, parsed, or already has an active recording. * * When `null` (default), playback failures fall through to [onSessionFailed] for * backwards compatibility. */ onPlaybackFailed: ((exception: Exception) -> Unit)? = null, /** * Updates of the state of the ARCore system. * Invoked once per [Frame] immediately before the Scene is updated. */ onSessionUpdated: ((session: Session, frame: Frame) -> Unit)? = null, /** * Listen for camera tracking failure. */ onTrackingFailureChanged: ((trackingFailureReason: TrackingFailureReason?) -> Unit)? = null, /** * The listener invoked for all the gesture detector callbacks. */ onGestureListener: GestureDetector.OnGestureListener? = rememberOnGestureListener(), onTouchEvent: ((e: MotionEvent, hitResult: HitResult?) -> Boolean)? = null, permissionHandler: ARPermissionHandler? = (LocalContext.current as? androidx.activity.ComponentActivity)?.let { activity -> remember(activity) { ActivityARPermissionHandler(activity) } }, lifecycle: Lifecycle = LocalLifecycleOwner.current.lifecycle, /** * DSL block for declaring AR nodes via [ARSceneScope]. */ content: (@Composable ARSceneScope.() -> Unit)? = null )</ID>
    <ID>LongMethod:ARSessionFailureTest.kt:ARSessionFailureTest$@Test fun `from() maps every ARCore exception subclass to its typed ARSessionFailure subtype`()</ID>
    <ID>LongMethod:AugmentedFaceNode.kt:AugmentedFaceNode$override fun update(trackable: AugmentedFace?)</ID>
    <ID>LongMethod:DepthMeshCollision.kt:@Suppress("UNUSED_PARAMETER") // y reserved for a future 3D variant — keep stable API internal fun nearestSurfaceYBelow( positionsWorld: FloatArray, indices: IntArray, x: Float, y: Float, z: Float, radius: Float, ): Float?</ID>
    <ID>LongMethod:DepthMeshGeometry.kt:internal fun buildDepthMeshGeometry( depthBuffer: ShortBuffer, depthWidth: Int, depthHeight: Int, rowStrideShorts: Int, fx: Float, fy: Float, cx: Float, cy: Float, stride: Int = DEFAULT_DEPTH_MESH_STRIDE, edgeThresholdMeters: Float = DEFAULT_DEPTH_EDGE_THRESHOLD_METERS, ): DepthMeshGeometry</ID>
    <ID>LongMethod:LightEstimator.kt:LightEstimator$fun update(session: Session, frame: Frame, camera: Camera): Estimation?</ID>
    <ID>LongMethod:LightEstimatorConcurrentDestroyTest.kt:LightEstimatorConcurrentDestroyTest$@Test fun `100 iterations of concurrent update + destroy at 50 percent — zero crashes`()</ID>
    <ID>LoopWithTooManyJumpStatements:DepthMeshCollision.kt:while</ID>
    <ID>LoopWithTooManyJumpStatements:RerunBridge.kt:RerunBridge$while</ID>
    <ID>LoopWithTooManyJumpStatements:XrHandNode.kt:for</ID>
    <ID>MatchingDeclarationName:ARDepthOfField.kt:ARDepthOfFieldOptions</ID>
    <ID>MatchingDeclarationName:ARFogNode.kt:ARFogConfig</ID>
    <ID>MatchingDeclarationName:ArSession.kt:ARSession : Session</ID>
    <ID>MatchingDeclarationName:CameraConfigFilterDsl.kt:CameraConfigFilterBuilder</ID>
    <ID>MatchingDeclarationName:Geospatial.kt:GeospatialPoseSnapshot</ID>
    <ID>MaxLineLength:ARCompletenessDefaultsTest.kt:ARCompletenessDefaultsTest$"""DisposableEffect\(fillLightNode\)\s*\{\s*fillLightNode\?\.let\s*\{\s*nodeManager\.addNode\(it\)\s*\}\s*onDispose\s*\{\s*fillLightNode\?\.let\s*\{\s*nodeManager\.removeNode\(it\)\s*\}\s*\}\s*\}"""</ID>
    <ID>MaxLineLength:ARCompletenessDefaultsTest.kt:ARCompletenessDefaultsTest$"ARSceneView's fill-light wire-up MUST be a `DisposableEffect(fillLightNode) { add ; onDispose { remove } }` "</ID>
    <ID>MaxLineLength:ARCompletenessDefaultsTest.kt:ARCompletenessDefaultsTest$val</ID>
    <ID>MaxLineLength:ARCore.kt:ARCore$ReplaceWith("create(context, (context as? androidx.activity.ComponentActivity)?.let { ActivityARPermissionHandler(it) }, features)")</ID>
    <ID>MaxLineLength:ARCore.kt:ARCore$ReplaceWith("resume(context, (context as? androidx.activity.ComponentActivity)?.let { ActivityARPermissionHandler(it) })")</ID>
    <ID>MaxLineLength:ARScene.kt:@Deprecated("Use ARSceneView instead", ReplaceWith("ARSceneView(modifier, surfaceType, engine, modelLoader, materialLoader, environmentLoader, sessionFeatures, playbackDataset, sessionCameraConfig, flashMode, sessionConfiguration, planeRenderer, cameraStream, view, isOpaque, renderer, scene, environment, mainLightNode, fillLightNode, cameraNode, cameraExposure, collisionSystem, viewNodeWindowManager, onSessionCreated, onSessionResumed, onSessionPaused, onSessionFailed, onPlaybackFailed, onSessionUpdated, onTrackingFailureChanged, onGestureListener, onTouchEvent, permissionHandler, lifecycle, content)"))</ID>
    <ID>MaxLineLength:ARScene.kt:permissionHandler</ID>
    <ID>MaxLineLength:ARTypedConfigModesTest.kt:ARTypedConfigModesTest$"""LaunchedEffect\($name\)\s*\{[^}]*session\.configure\s*\{\s*config\s*->\s*config\.$name\s*=\s*$name\s*\}"""</ID>
    <ID>MaxLineLength:ARTypedConfigModesTest.kt:ARTypedConfigModesTest$"""planeFindingMode\s*:\s*Config\.PlaneFindingMode\s*=\s*Config\.PlaneFindingMode\.HORIZONTAL_AND_VERTICAL"""</ID>
    <ID>MaxLineLength:ARTypedConfigModesTest.kt:ARTypedConfigModesTest$"""streetscapeGeometryMode\s*:\s*Config\.StreetscapeGeometryMode\s*=\s*Config\.StreetscapeGeometryMode\.DISABLED"""</ID>
    <ID>MaxLineLength:DepthMeshNode.kt:DepthMeshNode$boundingBox = Box(0f, 0f, 0f, DEGENERATE_AABB_HALF_EXTENT_M, DEGENERATE_AABB_HALF_EXTENT_M, DEGENERATE_AABB_HALF_EXTENT_M)</ID>
    <ID>MaxLineLength:RerunWireFormatEdgeCaseTest.kt:RerunWireFormatEdgeCaseTest$assertFalse("timestamp must not use 'E' notation", line.contains("E") &amp;&amp; line.indexOf("E") &lt; line.indexOf("\"type\""))</ID>
    <ID>NestedBlockDepth:ARScene.kt:private fun onARFrame( engine: Engine, scene: Scene, view: View, cameraNode: ARCameraNode, cameraStream: ARCameraStream?, lightEstimator: LightEstimator?, mainLightNode: LightNode?, environment: Environment, arPlaneRenderer: PlaneRenderer, childNodes: List&lt;Node>, prevTrackingFailureRef: AtomicReference&lt;TrackingFailureReason?>, onTrackingFailureChangedRef: AtomicReference&lt;((TrackingFailureReason?) -> Unit)?>, onSessionUpdatedRef: AtomicReference&lt;((Session, Frame) -> Unit)?>, baselineMainLightColorRef: AtomicReference&lt;io.github.sceneview.math.Color?>, baselineMainLightIntensityRef: AtomicReference&lt;Float?>, builtIndirectLightRef: AtomicReference&lt;IndirectLight?>, session: Session, frame: Frame )</ID>
    <ID>NestedBlockDepth:LightEstimator.kt:LightEstimator$fun update(session: Session, frame: Frame, camera: Camera): Estimation?</ID>
    <ID>NestedBlockDepth:PlaneRenderer.kt:PlaneRenderer$fun update(session: Session, frame: Frame)</ID>
    <ID>NewLineAtEndOfFile:ARCameraNode.kt:io.github.sceneview.ar.node.ARCameraNode.kt</ID>
    <ID>NewLineAtEndOfFile:ARCameraStream.kt:io.github.sceneview.ar.camera.ARCameraStream.kt</ID>
    <ID>NewLineAtEndOfFile:Anchor.kt:io.github.sceneview.ar.scene.Anchor.kt</ID>
    <ID>NewLineAtEndOfFile:ArSession.kt:io.github.sceneview.ar.arcore.ArSession.kt</ID>
    <ID>NewLineAtEndOfFile:AugmentedImage.kt:io.github.sceneview.ar.arcore.AugmentedImage.kt</ID>
    <ID>NewLineAtEndOfFile:AugmentedImageNode.kt:io.github.sceneview.ar.node.AugmentedImageNode.kt</ID>
    <ID>NewLineAtEndOfFile:Camera.kt:io.github.sceneview.ar.arcore.Camera.kt</ID>
    <ID>NewLineAtEndOfFile:CloudAnchorNode.kt:io.github.sceneview.ar.node.CloudAnchorNode.kt</ID>
    <ID>NewLineAtEndOfFile:Frame.kt:io.github.sceneview.ar.arcore.Frame.kt</ID>
    <ID>NewLineAtEndOfFile:HitResultNode.kt:io.github.sceneview.ar.node.HitResultNode.kt</ID>
    <ID>NewLineAtEndOfFile:LightEstimator.kt:io.github.sceneview.ar.light.LightEstimator.kt</ID>
    <ID>NewLineAtEndOfFile:PlaneRenderer.kt:io.github.sceneview.ar.scene.PlaneRenderer.kt</ID>
    <ID>NewLineAtEndOfFile:Pose.kt:io.github.sceneview.ar.arcore.Pose.kt</ID>
    <ID>NewLineAtEndOfFile:RooftopAnchorNode.kt:io.github.sceneview.ar.node.RooftopAnchorNode.kt</ID>
    <ID>NewLineAtEndOfFile:StreetscapeGeometryNode.kt:io.github.sceneview.ar.node.StreetscapeGeometryNode.kt</ID>
    <ID>NewLineAtEndOfFile:TerrainAnchorNode.kt:io.github.sceneview.ar.node.TerrainAnchorNode.kt</ID>
    <ID>NewLineAtEndOfFile:Trackable.kt:io.github.sceneview.ar.arcore.Trackable.kt</ID>
    <ID>SwallowedException:ARRecorder.kt:ARRecorder$e: RuntimeException</ID>
    <ID>SwallowedException:ARRecorder.kt:e: RuntimeException</ID>
    <ID>SwallowedException:CameraConfigFilterDsl.kt:e: RuntimeException</ID>
    <ID>SwallowedException:DepthMeshNodeUploadThrowLeakTest.kt:DepthMeshNodeUploadThrowLeakTest.ThrowingUploader$t: Throwable</ID>
    <ID>TooGenericExceptionThrown:RerunRateLimiterTest.kt:RerunRateLimiterTest$throw RuntimeException("server thread failed: ${e.message}", e)</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:// Environment-aware AR fog (issue #1717). Parameter names match the // `camera_stream_depth.mat` declarations 1:1 so the Filament binding is a // straight pass-through. Defaults mirror `FogNode`'s virtual-fog defaults so // real + virtual fog visually align out of the box. const val kFogEnabledParameter = "fogEnabled"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kCameraTextureParameter = "cameraTexture"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kDepthTextureParameter = "depthTexture"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kFogColorParameter = "fogColor"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kFogDensityParameter = "fogDensity"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kFogEndParameter = "fogEnd"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kFogStartParameter = "fogStart"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:// People occlusion (issue #1761). The `personTexture` sampler holds the binary PERSON mask // derived from ARCore Scene Semantics — see `camera_stream_person_occlusion.mat`. const val kPersonTextureParameter = "personTexture"</ID>
    <ID>TopLevelPropertyNaming:ARCameraStream.kt:const val kUVTransformParameter = "uvTransform"</ID>
    <ID>TopLevelPropertyNaming:ARCore.kt:/** * Assumed distance in meters from the device camera to the surface on which the user will * try to place models. * * This value affects the apparent scale of objects while the tracking method of the Instant * Placement point is `SCREENSPACE_WITH_APPROXIMATE_DISTANCE`. Values in the [0.2, 2.0] meter * range are a good choice for most AR experiences. */ const val kDefaultHitTestInstantDistance = 2.0f</ID>
    <ID>UnusedParameter:ARRecorderTest.kt:ARRecorderTest.ShadowCameraConfigFilter$direction: CameraConfig.FacingDirection?</ID>
    <ID>UnusedParameter:ARRecorderTest.kt:ARRecorderTest.ShadowCameraConfigFilter$targetFps: java.util.EnumSet&lt;CameraConfig.TargetFps>?</ID>
    <ID>UnusedParameter:ARScene.kt:/** * Used for [io.github.sceneview.node.ViewNode]s that can display an Android [android.view.View]. */ viewNodeWindowManager: WindowManager? = null</ID>
    <ID>UnusedParameter:ARScene.kt:view: View</ID>
    <ID>UnusedParameter:DepthMeshGeometryTest.kt:DepthMeshGeometryTest$width: Int</ID>
    <ID>UnusedParameter:Frame.kt:trackable: T</ID>
    <ID>UnusedParameter:PlaneRenderer.kt:PlaneRenderer$session: Session</ID>
    <ID>UnusedParameter:RerunBridge.kt:RerunBridge$session: Session</ID>
  </CurrentIssues>
</SmellBaseline>
