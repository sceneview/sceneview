<!-- category: Fixed -->

- **iOS demo: the About identity block sits flat on the page and uses the launcher icon ([#3808](https://github.com/sceneview/sceneview/issues/3808)).** The glass card around the mark, name, version and tagline is gone, so the support card is again the one emphasised surface of the screen. The mark is the app icon at 80 pt with `radius-xl` corners, the same picture in light and dark, instead of an SF Symbol cube on a gradient tile. The version reads as plain text instead of a material pill. This matches the Android screen (#3564).
