<!-- category: Fixed -->
- CI: the release-fast bump step no longer aborts when `sync-versions.sh --fix` exits 1 after applying fixes — the fast-release pipeline completes the bump instead of failing spuriously ([#2661](https://github.com/sceneview/sceneview/pull/2661))
