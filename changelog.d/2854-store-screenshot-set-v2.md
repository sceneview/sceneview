<!-- category: Changed -->
- Reworked the common store-screenshot set shared by the Play Store and App Store capture scripts: `materials` now leads (the only frame with a visible HDRI environment), `lighting` is dropped as a near-duplicate of `model-viewer`, `fog` takes its slot, and the order puts three distinct capabilities — rendering, model loading, physics — in the search-visible top three instead of three shots of the same helmet. Every id was re-verified as a standalone demo on both platforms (own Android `Fragment`, own iOS `@sceneId`), so no two slots can collapse onto the same demo the way they did before #2773 (#2854).
