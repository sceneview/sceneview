<?xml version="1.0" encoding="utf-8"?>
<!-- Strings for the `ar-streetscape` demo (registered by
     `ArStreetscapeFragment.kt`). Android's resource merger
     fans every `res/values/*.xml` file in, so `R.string.demo_ar_streetscape_title` etc.
     remains globally resolvable. Adding a new demo never needs to touch
     this file or `strings.xml` — see #1870. -->
<resources>
    <string name="demo_ar_streetscape_title">Streetscape Geometry</string>
    <string name="demo_ar_streetscape_subtitle">Geospatial building and terrain meshes</string>
    <!-- Shown after ~15 s of tracking with no geometry found and no unsupported-device
         error — the supported-device / no-VPS-coverage case (#1615). Streetscape can
         never find geometry indoors, so the demo must explicitly tell the user to go
         outside rather than spinning on "Looking for streetscape geometry…" forever. -->
    <string name="demo_ar_streetscape_no_geometry_hint">No streetscape geometry found. This demo needs an outdoor location with Google Street View coverage — point at nearby buildings. It will not find anything indoors.</string>
</resources>
