<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required by the Play Core In-App Updates SDK so the activity can
         check the Play Store track on resume — see InAppUpdateManager in
         :samples:common. Without this, AppUpdateManager.appUpdateInfo
         silently fails with NETWORK_ERROR and the banner never appears. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Leanback is declared but not required: Play serves the app to Android TV / Google TV
         from the LEANBACK_LAUNCHER intent and the banner below, and `required="false"` is what
         the Play TV quality checks ask for so the same APK could also run elsewhere. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- No touchscreen on a TV: the D-pad drives everything. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:label="@string/app_name"
        android:theme="@style/Theme.SceneViewSample">
        <activity
            android:name=".TvModelViewerActivity"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|navigation"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
